[//]: # ( <auto-generated> [xCodeGen.Hash: 359c5e04f75d9c99e27723bca020d60291b4b8865cc01c858c083b0b7cde42bb] </auto-generated> )

# 领域数据地图 (Domain Map)
> **自动生成** — 由 xCodeGen 在编译后按需增量更新。手动修改将被覆盖。
> 生成时间：2026-09-11 08:51:36
>
> 🔒 **硬性禁止读 `*.g.cs` / `Entities\*.cs` / `DataServices\*.cs`**：本文档已完整列出全部实体与字段；生成源码含大量无关代码且不可靠。若本文档缺失或过时，报告补齐，不得绕开去读生成文件。

---

## 一、子域与实体

> **命名公约**：名称以 `Dto` 结尾的类为对应 `Entity` 的 DTO；以 `DataService` 结尾的类为对应实体的数据服务。
> **实体 vs 视图**：实体支持 CRUD；视图只能查询和统计。
> **字段列表**：见下方各实体字段明细表（可空列 `Y`=可空 / `N`=非空，字段说明来自 XML Doc Comment）。

<!-- @xcodegen:begin entity-table -->

<!--
  xCodeGen 模板生成范围（从实体类提取）：
  - 实体:      类名
  - 类型:      实体 / 视图（从基类推断）
  - DataService: 对应 DataService 类名
  - 子域:      [SubDomain("xxx")] 特性值
  - 描述:      /// <summary> 内容（前 60 字）
  - 字段:      /// <summary> + 可空性（从实体属性提取）
-->

### 子域：默认

| 实体 | 类型 | DataService | 描述 |
|:---|:---|:---|:---|
| AiModelConfig | 实体 | `-` | AI 模型配置（统一 AI 网关路由表） |
| Attempts | 实体 | `-` | 作答记录（唯一事实源） |
| Banks | 实体 | `-` | 题库元数据（API 查询索引） |
| BetaInviteCodes | 实体 | `-` | 内测邀请码（平台发放给群主） |
| DailyStats | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： DailyStats.Conditions.ByXxx(value)... |
| GroupMembers | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： GroupMembers.Conditions.ByXxx(value)... |
| Groups | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： Groups.Conditions.ByXxx(value) —... |
| JudgmentFeedback | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： JudgmentFeedback.Conditions.ByXxx(va... |
| KnowledgeMastery | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： KnowledgeMastery.Conditions.ByXxx(va... |
| MemoryStates | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： MemoryStates.Conditions.ByXxx(value)... |
| OneTimeInviteCodes | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： OneTimeInviteCodes.Conditions.ByXxx(... |
| ParentStudentRelations | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： ParentStudentRelations.Conditions.By... |
| PkAttempts | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： PkAttempts.Conditions.ByXxx(value)... |
| PkMatches | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： PkMatches.Conditions.ByXxx(value) —... |
| PkPlayers | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： PkPlayers.Conditions.ByXxx(value) —... |
| Questions | 实体 | `-` | 题目索引表（API 查询索引） |
| RankSnapshots | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： RankSnapshots.Conditions.ByXxx(value... |
| RosterImports | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： RosterImports.Conditions.ByXxx(value... |
| StudyBuddies | 实体 | `-` | 学习搭子（双向同意制，同一对用户仅一条关系记录） |
| StudySessions | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： StudySessions.Conditions.ByXxx(value... |
| Subscriptions | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： Subscriptions.Conditions.ByXxx(value... |
| TaskAssignments | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： TaskAssignments.Conditions.ByXxx(val... |
| Tasks | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： Tasks.Conditions.ByXxx(value) —... |
| WrongQuestions | 实体 | `-` | 条件工厂（V4.9）。支持两种使用方式： WrongQuestions.Conditions.ByXxx(valu... |

#### AiModelConfig（实体）

> DataService：`-` ｜ AI 模型配置（统一 AI 网关路由表）

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| Name | `string` | N | 配置名（≤64） |
| Provider | `AiProvider` | N | 供应商（OpenAiCompatible） |
| BaseUrl | `string` | N | 在线 API BaseUrl（如 https://api.openai.com/v1） |
| ApiKey | `string` | N | API Key（敏感：不下发前端、不入日志） |
| ModelName | `string` | N | 模型名（如 gpt-4o-mini） |
| Enabled | `bool` | N | 是否启用（默认 true） |
| SortOrder | `int` | N | 轮询顺序（升序优先，默认 0） |
| TimeoutSeconds | `int` | N | 请求超时秒数（默认 30） |
| Remark | `string?` | Y | 备注（可空） |

#### Attempts（实体）

> DataService：`-` ｜ 作答记录（唯一事实源）

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| UserId | `long` | N | 学生用户 |
| SessionId | `long?` | Y | 学习会话归属 |
| QuestionId | `string` | N | 题目业务键（Q-ch-7a-0001） |
| BankId | `string` | N | 题库业务键 |
| Scenario | `LearningScenario` | N | 场景（LearningScenario，由会话派生） |
| QType | `string` | N | 题型键（R1/R2/R3a/R3b/O1...） |
| PreState | `MemoryState` | N | 作答前记忆状态（MemoryState） |
| PostState | `MemoryState` | N | 作答后记忆状态（MemoryState） |
| Result | `JudgmentResult` | N | 判题结果（Correct/Partial/Wrong） |
| Confidence | `double?` | Y | 判题置信度（0-1） |
| HintLevel | `HintLevel` | N | 求助档位（None/Partial/Full） |
| TimeCostMs | `int?` | Y | 作答耗时（服务端计时，毫秒） |
| AnsweredAt | `DateTime` | N | 作答时间 |

#### Banks（实体）

> DataService：`-` ｜ 题库元数据（API 查询索引）

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| BankId | `string` | N | 业务键（chinese-7to9-pep） |
| Name | `string` | N | 题库名（≤128） |
| Subject | `Subject` | N | 学科 |
| Version | `string` | N | 内容版本（MinVer） |
| Purpose | `BankPurpose` | N | 用途（Memorize/Assess/Play） |
| Privacy | `BankPrivacy` | N | 隐私（Private/Public/Group） |
| OwnerId | `long?` | Y | 私域题库 owner（官方题库 NULL） |
| JsonPath | `string` | N | bank.v1.json 文件路径（内容权威） |
| Tags | `string[]` | N | 标签（JSONB，如 ["高频考点"]） |
| Status | `BankStatus` | N | 状态（Active/Hidden/Archived） |

#### BetaInviteCodes（实体）

> DataService：`-` ｜ 内测邀请码（平台发放给群主）

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| Code | `string` | N | 内测邀请码（平台生成，16 位） |
| Status | `BetaCodeStatus` | N | 状态（Pending/Used/Expired） |
| IssuedToUserId | `long?` | Y | 发放对象（群主，可 NULL=未指定） |
| IssuedAt | `DateTime?` | Y | 发放时间 |
| ExpiresAt | `DateTime` | N | 内测期码有效期 |
| UsedAt | `DateTime?` | Y | 群主激活建群时间 |
| UsedByGroupId | `long?` | Y | 激活的群组 |

#### DailyStats（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： DailyStats.Conditions.ByXxx(value)...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| UserId | `long` | N | 学生用户 |
| StatDate | `DateOnly` | N | UTC+8 业务日期 |
| LearnedCount | `int` | N | 学习题数 |
| StarredCount | `int` | N | 点亮★数（热力图） |
| ReviewCount | `int` | N | 复习题数 |
| Accuracy | `double?` | Y | 当日正确率 |
| StudySeconds | `int` | N | 学习时长（秒） |

#### GroupMembers（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： GroupMembers.Conditions.ByXxx(value)...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| GroupId | `long` | N | 所属群组 |
| UserId | `long` | N | 成员用户 |
| Role | `MemberRole` | N | 成员角色（Student/Parent） |
| Nickname | `string?` | Y | 群内昵称（学生号/姓名） |
| InviteCodeId | `long?` | Y | 加入所用一次性邀请码（溯源） |
| JoinedAt | `DateTime` | N | 加入时间 |

#### Groups（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： Groups.Conditions.ByXxx(value) —...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| OwnerId | `long` | N | 群主（原老师/班主任） |
| Name | `string` | N | 群组名称（≤64，如"七(3)班"） |
| Subject | `string` | N | 学科（history/geography/biology/daodeyufazhi） |
| Grade | `string?` | Y | 年级（七年级/八年级/九年级） |
| Status | `GroupStatus` | N | 状态（Active/Archived） |
| RankEnabled | `bool` | N | 群组战绩榜开关（false → 战绩榜入口隐藏） |
| BetaCodeId | `long?` | Y | 激活本群所用的内测邀请码 |

#### JudgmentFeedback（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： JudgmentFeedback.Conditions.ByXxx(va...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| AttemptId | `long` | N | 关联作答记录（跨模块，学习域） |
| UserId | `long` | N | 反馈用户 |
| FeedbackType | `FeedbackType` | N | 反馈类型（WrongJudgment/Other） |
| Status | `FeedbackStatus` | N | 复核状态（Pending/Reviewed/Resolved） |
| ReviewedBy | `string?` | Y | 复核人（人工队列） |
| ReviewedAt | `DateTime?` | Y | 复核时间 |

#### KnowledgeMastery（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： KnowledgeMastery.Conditions.ByXxx(va...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| UserId | `long` | N | 学生用户 |
| Subject | `string` | N | 学科 |
| KnowledgePoint | `string` | N | 知识点 |
| State | `MemoryState` | N | 聚合状态（取题目中位/最差，MemoryState） |
| Accuracy | `double` | N | 聚合正确率 |
| AttemptCount | `int` | N | 作答次数 |
| LastReviewedAt | `DateTime?` | Y | 最近复习时间 |

#### MemoryStates（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： MemoryStates.Conditions.ByXxx(value)...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| UserId | `long` | N | 学生用户 |
| QuestionId | `string` | N | 题目业务键 |
| BankId | `string` | N | 题库业务键 |
| State | `MemoryState` | N | 记忆状态（NotMastered/Fuzzy/Mastered/Proficient） |
| ConsecutiveCorrect | `int` | N | 连续独立答对（2 次→★） |
| HistoryAccuracy | `double` | N | 近 20 次正确率（间隔系数 0.5~1.5） |
| EaseFactor | `double` | N | 预留（Phase2 SM-2） |
| NextReviewAt | `DateTime` | N | 下次复习（驱动队列） |
| LastAttemptId | `long?` | Y | 最近一次作答 |
| LastHintLevel | `HintLevel` | N | 最近求助档位（None/Partial/Full） |

#### OneTimeInviteCodes（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： OneTimeInviteCodes.Conditions.ByXxx(...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| GroupId | `long` | N | 所属群组 |
| Code | `string` | N | 一次性邀请码（8 位） |
| PhoneLast4 | `string` | N | 名单手机号后四位（脱敏定向标识） |
| Status | `OneTimeCodeStatus` | N | 状态（Unused/Used/Expired） |
| GeneratedBy | `long` | N | 群主（生成者） |
| RosterImportId | `long?` | Y | 来源名单批次 |
| GeneratedAt | `DateTime` | N | 生成时间 |
| ExpiresAt | `DateTime?` | Y | 有效期（可空：内测期默认 30 天） |
| UsedAt | `DateTime?` | Y | 激活时间 |
| BoundUserId | `long?` | Y | 激活绑定（成员微信 OpenId → UserId） |

#### ParentStudentRelations（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： ParentStudentRelations.Conditions.By...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| ParentId | `long` | N | 家长 |
| StudentId | `long` | N | 孩子 |
| Relation | `ParentRelation` | N | 关系（Parent/Guardian/Grandparent） |

#### PkAttempts（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： PkAttempts.Conditions.ByXxx(value)...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| MatchId | `long` | N | 对局归属 |
| PlayerId | `long` | N | 参赛者成绩记录 |
| UserId | `long` | N | 答题用户 |
| QuestionId | `string` | N | 题目业务键 |
| Answer | `string?` | Y | 用户答案 |
| Result | `PkAttemptResult` | N | 判题结果（Correct/Partial/Wrong） |
| IsCorrect | `bool` | N | 答对标记（Correct=true；Partial/Wrong=false） |
| TimeCostMs | `int` | N | 答题用时（毫秒，客户端上送） |

#### PkMatches（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： PkMatches.Conditions.ByXxx(value) —...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| Subject | `Subject` | N | 学科 |
| BankId | `string` | N | 题库业务键 |
| Topic | `string?` | Y | 知识点/主题（按知识点自动出题） |
| QuestionCount | `int` | N | 题量（5/10/20） |
| PerQuestionTimeS | `int` | N | 每题限时（秒） |
| TotalTimeLimitS | `int` | N | 总时长上限（秒） |
| Mode | `PkMode` | N | 模式（Sync/Async） |
| Status | `PkMatchStatus` | N | 状态（Pending/Ongoing/Finished/Cancelled） |
| WinnerId | `long?` | Y | 胜方（NULL=平局） |
| InviteCode | `string?` | Y | 4 位对战码（方式 B 加入） |
| FinishReason | `PkFinishReason?` | Y | 结束原因（Score/Forfeit/Timeout） |
| FinishedAt | `DateTime?` | Y | 结束时间 |

#### PkPlayers（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： PkPlayers.Conditions.ByXxx(value) —...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| MatchId | `long` | N | 对局归属 |
| UserId | `long` | N | 参赛者 |
| Score | `int` | N | 总分（每题 +10） |
| CorrectCount | `int` | N | 答对数 |
| TotalTimeMs | `int` | N | 总用时（毫秒，同分比用时） |
| AiComment | `string?` | Y | AI 趣味点评（失败留空 + 兜底文案） |

#### Questions（实体）

> DataService：`-` ｜ 题目索引表（API 查询索引）

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| QuestionId | `string` | N | 业务键（Q-ch-7a-0001） |
| BankId | `string` | N | 题库业务键（外键引用 Banks.BankId） |
| ChapterId | `string?` | Y | 章节/单元（如 7a） |
| QType | `QuestionType` | N | 题型键（R1/R2/O1...） |
| Content | `string` | N | 题目展示内容镜像（JSON，不含答案，防爬） |
| Keywords | `string` | N | KeywordGroup[] 判题关键词（JSON，服务端校验用，不下发） |
| KnowledgePoints | `string[]` | N | 知识点列表（JSONB GIN 索引） |
| Difficulty | `int` | N | 难度 0-5 |
| Status | `QuestionStatus` | N | 状态（Active/Superseded/Hidden） |
| SupersededBy | `string?` | Y | 题目改版替换（软删除） |

#### RankSnapshots（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： RankSnapshots.Conditions.ByXxx(value...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| UserId | `long` | N | 用户 |
| ScopeType | `RankScopeType` | N | 范围类型（Group/Grade） |
| ScopeId | `string?` | Y | group_id（群组）或 grade_key（年级） |
| Subject | `string` | N | 学科筛选维度（All/语文/…/政治） |
| MetricType | `RankMetricType` | N | 指标类型（Streak/Volume/PkWins/Accuracy/Mastery/Stars） |
| MetricValue | `decimal` | N | 冻结的指标原始值 |
| Rank | `int` | N | 冻结的排名（不回溯） |
| SnapshotDate | `DateOnly` | N | 快照日期 |

#### RosterImports（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： RosterImports.Conditions.ByXxx(value...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| GroupId | `long` | N | 所属群组 |
| OwnerId | `long` | N | 群主 |
| ImportMethod | `string` | N | 导入方式（Paste(批量粘贴) / File(文件导入)） |
| SourceCount | `int` | N | 原始行数 |
| CleanedCount | `int` | N | Agent 整理后有效数（去重/格式校验） |
| DuplicateCount | `int` | N | 去重剔除数 |
| InvalidCount | `int` | N | 非法手机号剔除数 |
| Status | `RosterImportStatus` | N | 状态（Processing/Ready/Exported/Failed） |
| CsvFileUrl | `string?` | Y | 导出的 CSV（OSS） |
| CsvExpiresAt | `DateTime?` | Y | CSV 有效期（建议 7 天） |
| CompletedAt | `DateTime?` | Y | 整理完成时间 |
| RawPhonesJson | `string?` | Y | 名单手机号原始列表（JSON 数组，jsonb）。 导入时存原始列表；Agent 整理后覆盖为清洗后列表（去重... |

#### StudyBuddies（实体）

> DataService：`-` ｜ 学习搭子（双向同意制，同一对用户仅一条关系记录）

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| InviterId | `long` | N | 邀请方 |
| InviteeId | `long` | N | 被邀请方 |
| Status | `BuddyStatus` | N | 状态（Pending/Accepted/Rejected/Removed/Expired） |
| InvitedAt | `DateTime` | N | 邀请时间 |
| AcceptedAt | `DateTime?` | Y | 同意时间 |
| ExpiresAt | `DateTime` | N | 邀请过期（InvitedAt + 7 天） |

#### StudySessions（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： StudySessions.Conditions.ByXxx(value...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| UserId | `long` | N | 学生用户 |
| Scenario | `LearningScenario` | N | 场景（Memorize/Assess/PlayPk/PlayDaily） |
| BankId | `string` | N | 题库业务键（跨模块引用 Banks） |
| SessionType | `SessionType` | N | 会话类型（Progressive/Free/Assembled/Level/Pk） |
| QuestionCount | `int` | N | 计划题数 |
| CorrectCount | `int` | N | 本次答对题数 |
| TotalTimeMs | `int` | N | 累计用时（毫秒） |
| TaskId | `long?` | Y | 群组任务归属（个人背诵 NULL） |
| StartedAt | `DateTime` | N | 会话开始时间 |
| EndedAt | `DateTime?` | Y | 会话结束时间 |

#### Subscriptions（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： Subscriptions.Conditions.ByXxx(value...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| ParentId | `long` | N | 家长 |
| StudentId | `long` | N | 孩子（报告对象） |
| Plan | `SubscriptionPlan` | N | 方案（Month/Year） |
| Status | `SubscriptionStatus` | N | 状态（Trialing/Active/Expired/Cancelled） |
| TrialEndAt | `DateTime?` | Y | 试用结束（试用期 7 天） |
| PeriodEndAt | `DateTime?` | Y | 当前计费周期结束 |

#### TaskAssignments（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： TaskAssignments.Conditions.ByXxx(val...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| TaskId | `long` | N | 所属任务 |
| UserId | `long` | N | 学生 |
| Status | `AssignmentStatus` | N | 状态（Pending/InProgress/Completed/Overdue） |
| Progress | `int` | N | 完成度 0-100（已完成题数/总题数） |
| SessionId | `long?` | Y | 关联学习会话（执行任务时创建，跨模块） |
| AssignedAt | `DateTime` | N | 分配时间 |
| StartedAt | `DateTime?` | Y | 首次开始时间 |
| CompletedAt | `DateTime?` | Y | 全部完成时间 |

#### Tasks（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： Tasks.Conditions.ByXxx(value) —...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键（uuid，API/DTO 暴露） |
| OwnerId | `long` | N | 群主（发布者） |
| GroupId | `long` | N | 目标群组 |
| BankId | `string?` | Y | 关联题库（官方/PGC/自定义，业务键） |
| Title | `string` | N | 任务标题（≤128） |
| Description | `string?` | Y | 任务说明 |
| QuestionIds | `string[]` | N | 题目 ID 列表（jsonb，任务题集） |
| QuestionCount | `int` | N | 题目数量 |
| Scenario | `TaskScenario` | N | 场景（Memorize/Assess） |
| SessionType | `TaskSessionType` | N | 会话类型（Progressive/Free/Assembled） |
| AllowRedo | `bool` | N | 是否允许重做（重做不改变 Completed 状态） |
| StartedAt | `DateTime` | N | 任务开始时间 |
| DeadlineAt | `DateTime?` | Y | 截止时间（NULL=无截止） |
| Status | `TaskStatus` | N | 状态（Draft/Active/Closed） |

#### WrongQuestions（实体）

> DataService：`-` ｜ 条件工厂（V4.9）。支持两种使用方式： WrongQuestions.Conditions.ByXxx(valu...

| 字段 | 类型 | 可空 | 说明 |
|:---|:---|:---:|:---|
| Id | `long` | N | 自增主键 |
| UId | `string` | N | 外部业务键 |
| UserId | `long` | N | 学生用户 |
| QuestionId | `string` | N | 题目业务键 |
| BankId | `string` | N | 题库业务键 |
| Subject | `string` | N | 冗余学科字段（跨学科查询免 join） |
| WrongCount | `int` | N | 错误次数 |
| LastWrongAt | `DateTime` | N | 最近错误时间 |
| Mastered | `bool` | N | 连续 2 次答对 → true |


<!-- @xcodegen:end entity-table -->

> **DataService 映射**（实体 ↔ DataService）参见 [`DataService_API.md`](./DataService_API.md)（搜索能力索引中含对应关系）。
> **外部 API 契约**（GraphQL Query/Mutation、AOP 接口）参见 [`Service_API.md`](./Service_API.md)。
> **业务规则**参见 [`Business.md`](./Business.md)。