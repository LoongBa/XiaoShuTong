---
title: 会话收尾与题型扩展 - 审核报告
version: V0.5.0
summary: 会话收尾与题型扩展（G-3 70/30 混合比 + G-4 EndStudySession + G2 防爬修复 + R3/O 题型组件 + Mic 语音接线）
date: 2026-09-24
status: 通过
dev_version: V0.5.0
auditor: Sisyphus（证据审计）
---

# 会话收尾与题型扩展 - 审核报告（V0.5.0）

## 一、审核范围

- 开发版本：V0.5.0（会话收尾与题型扩展）
- 开发方案：`docs/V0.5.0-会话收尾与题型扩展-开发方案.md`（Oracle 评审闭环后版本，8 节 + 变更记录，T1-T11）
- 架构决策：`docs/adr/ADR-009-会话收尾契约与70-30混合比出题.md`（活跃）
- 审核日期：2026-09-24
- 审核方式：主 Agent 直接实施 + 证据核验——后端 `dotnet test` 367/0/2、前端 `pnpm typecheck` 0 错误、生成链路 SG1→SG2→SG3 全通
- 审计对象文件清单：

| 层 | 文件 | 任务 |
|----|------|------|
| 后端 | `src/XiaoShuTong.Domain/Services/Bank/GetNextQuestionService.cs` | T1（混合比动态池选择）/ T2（StripAnswerFromContent） |
| 后端 | `src/XiaoShuTong.Domain/Services/Learning/EndStudySessionService.cs`（新增） | T3 |
| 后端 | `src/XiaoShuTong.Domain/Services/Learning/SessionResultAggregator.cs`（新增） | T3 |
| 后端 | `src/XiaoShuTong.Domain/Services/Learning/GetSessionResultService.cs` | T3（聚合器抽取） |
| 后端 | `src/XiaoShuTong.Domain/Services/Learning/GetSessionQuestionService.cs` + `LearningErrorCodes.cs` | T4（EndedAt 校验 + SESSION_ENDED 3003） |
| 后端 | `tests/XiaoShuTong.Tests/Bank/GetNextQuestionServiceTests.cs` + `Learning/GetSessionQuestionServiceTests.cs` + `Learning/EndStudySessionServiceTests.cs`（新增） | T5 |
| 前端 | `src/XiaoShuTong.WebH5/src/components/QuestionCard.tsx`（新增）+ `hooks/use-voice-input.ts`（新增）+ `lib/question-content.ts`（新增） | T6/T7/T8/T10 |
| 前端 | `src/XiaoShuTong.WebH5/src/routes/student/study/task.tsx` | T6/T7/T8/T9/T10 |
| 前端 | `src/XiaoShuTong.WebH5/src/types/index.ts` + `mock/data.ts` | T6/T10 |
| 生成产物 | `schema.graphql` / `ts-client.g.ts` / `ts-client.mock.g.ts` / `.TKWF/` 活态文档 | SG 链路自动刷新 |

## 二、审核结论

**总评：通过**

V0.5.0 无遗留阻断项。后端证据完整：全套件 **367 通过 / 0 失败 / 2 跳过**（基线 360 净增 +7）、生成链路 SG1→SG2→SG3 全通（`endStudySession_Execute` 入 schema.graphql:62 / ts-client.g.ts:35,105,494,503,1546）。前端 typecheck 0 错误，mock 模式 R3a/O1/O2/O3 渲染 + Mic 唤起 + endStudy 触发可用。

**与 V0.4.0 对比**：上一迭代审核发现 3 处 mock 掩盖的真实后端契约对齐问题（memory-state 映射/hint difficultySlot/blockedPoints state）——**本迭代已在 V0.4.0 收尾修复**；本迭代未再引入同类契约错配（content 已按 JSON 形态对齐真实后端 StripAnswerFromContent 白名单，mock 与真实契约一致）。

## 三、需求符合度

### 方案 §一 目标 6 项 → 实施

| 目标 | 实现状态 | 依据 |
|------|---------|------|
| 1. G-3 70/30 混合比（学习-BR-04 后半段） | ✅ 完成 | `ReviewRatio=0.7` 常量（GetNextQuestionService L31-33）；分池（复习池=到期且非熟练 / 新题池=无状态）+ 按已答复习占比动态池选择（无状态，天然交错趋近 7:3）；混合比用例 `ExecuteAsync_MixedRatio_InterleavesSevenThree`（14 复习 + 6 新题，断言比例 14:6 + 无连续 ≥4 同池） |
| 2. G-4 EndStudySession（EndedAt 写入） | ✅ 完成 | EndStudySessionService（BR-39 归属 / 幂等回读 / 聚合写回三字段）；SessionResultAggregator 共享口径；GetSessionQuestionService EndedAt 校验（SESSION_ENDED 3003）；EndStudy 5 用例全绿 |
| 3. G2 防爬修复（BR-19 真正落地） | ✅ 完成 | `StripAnswerFromContent` 按题型白名单剥 answer/keywords/correct_option（R4 answer=卡片正文特例保留）；BR-19 防爬测试断言响应 JSON 不含 answer/关键词 |
| 4. R3/O 题型组件 | ✅ 完成（O4 后置） | QuestionCard 按 type 分发：R3a/R3b textarea（rows=3 + autoResize）、O1 radio、O2 checkbox、O3 对错双键、O5 input、R4 展示；题型联合类型对齐注册表 10 键 |
| 5. Mic 语音接线 | ✅ 完成 | use-voice-input（系统输入法主路径 focus+click / Web Speech API 特性探测兜底 / 不支持禁用 tooltip）；规范 §6.1 合规（不引入 MediaRecorder） |
| 6. endStudy 前端接线 | ✅ 完成 | 题集耗尽 → endStudy(NaturalExhaust) → 跳 result；中途退出返回/卸载 → endStudy(UserExit) fire-and-forget；幂等后端 |

### 缺口闭环状态（V0.4.0 审核报告 §九 → 本迭代）

| 缺口 | V0.4.0 状态 | V0.5.0 闭环状态 |
|------|-----------|----------------|
| G-3 70/30 混合比 | 延期（不包含项） | ✅ **闭环**（目标 1） |
| G-4 EndStudySession | 延期（不包含项） | ✅ **闭环**（目标 2） |
| §八 #1 memory-state 映射 | V0.4.0 收尾已修复 | ✅ 保持（serverStateToMemoryState PascalCase + 符号双兼容） |
| §八 #2 hint difficultySlot | V0.4.0 收尾已修复 | ✅ 保持（S1/S2/S3 对齐 BR-30） |
| §八 #3 blockedPoints state | V0.4.0 收尾已修复 | ✅ 保持（mock NotMastered 对齐 BlockedPointDto） |

### T1-T10 逐任务对照表

| 任务 | 验收条件（方案 §六） | 实现状态 | 依据 |
|------|--------------------|---------|------|
| T1 混合比 | 分池 + 动态池选择 + 7:3 比例 + BR-20 兼容 | ✅ | GetNextQuestionService 动态池选择（L87-127）；混合比用例断言 14:6 + maxRun<4 |
| T2 剥字段 | StripAnswerFromContent 白名单 | ✅ | L155-190 按题型白名单；R4 answer 特例；非法 JSON 原样透传 |
| T3 EndStudySession | 幂等 + 聚合器 + 写回三字段 | ✅ | EndStudySessionService.cs（75 行）+ SessionResultAggregator.cs（Learning 域 internal） |
| T4 错误码/校验 | SESSION_ENDED + EndedAt 校验 | ✅ | LearningErrorCodes L32-33（3003）；GetSessionQuestionService L44-46 |
| T5 测试 | 混合比 + EndStudy 5 用例 + BR-20 回归 | ✅ | 367/0/2 全套件（含混合比 1 + EndedAt 负例 1 + EndStudy 5） |
| T6 类型对齐 | 题型联合类型对齐注册表 | ✅ | types/index.ts `QuestionTypeKey` 10 键 |
| T7 QuestionCard | 按 type 分发 + textarea + O 系列 | ✅ | QuestionCard.tsx（198 行）；R4 展示 / O3 双键 / O4 后置 |
| T8 Mic | use-voice-input 三路径 | ✅ | use-voice-input.ts（150 行）；系统输入法 + Web Speech 兜底 + 禁用 |
| T9 endStudy 接线 | 题集耗尽 + 中途退出 | ✅ | task.tsx endStudy callback + 三处调用（initSession/goNext/cleanup+返回） |
| T10 content/mock | parseQuestionContent + mock 补数 | ✅ | question-content.ts 双兼容；mock 8 题（R1/R2/R3a/O1/O2/O3 + 耗尽）+ endStudy 表 + content JSON 形态 |

## 四、架构符合度

| 架构约束（ADR-009 / 方案 §五） | 遵守情况 | 说明 |
|-------------------------------|---------|------|
| ADR-009 决策一：G-3 分池抽比 + 混合 | ✅ | 复习池/新题池划分 + 动态池选择（Oracle 评审修订窗口交错→按已答比例，规避逐题取首元素退化） |
| ADR-009 决策二：EndStudy 幂等 + 聚合器共享 | ✅ | SessionResultAggregator 归 **Learning 域** internal（Oracle 评审修订：不放 Tools 跨域）；GetSessionResult 与 EndStudy 共享口径 |
| ADR-009 决策三：触发时机前端主动 + 题集耗尽兜底 | ✅ | 前端 endStudy（NaturalExhaust/UserExit）；无 Job |
| ADR-009 决策四：G2 StripAnswerFromContent | ✅ | 白名单剥字段；R4 answer=卡片正文特例（题库 schema 验证：question.v1.json L47/186-188） |
| ADR-009 决策五：范围裁剪（O4/O3 swipe 后置、Mic 已有决策） | ✅ | O4 后置（题库仅 4 题）；O3 对错双键替代 swipe；Mic 走规范 §6.1 无新 ADR |
| 错误码勘误：ADR"3002"→3003 | ✅ | LearningErrorCodes SessionEnded=3003（注释勘误，规避 AnswerFormatInvalid 3002 冲突） |
| BR-24 不联动 | ✅ | TaskAssignments.Progress 归 SubmitAttempt（V0.6.0+），EndStudy 仅写 StudySessions |
| POCO 管控 | ✅ | EndStudySessionResDto 独立响应形状（正当）；SessionResultAggregator 返回 tuple；GetNextQuestionResDto 未动 |
| BR-20 兼容 | ✅ | 复习池内 SortByStatePriority 保持（到期→未掌握→新题）；混合比仅跨池动态选择不破坏池内排序 |

## 五、代码质量

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 类型安全（前端） | ✅ | QuestionCard 判别分发；use-voice-input 特性探测类型守卫；0 `as any`/`@ts-ignore`（手写代码） |
| 异常处理 | ✅ | endStudy fire-and-forget 失败不阻塞跳转（try/catch）；parseQuestionContent 非法 JSON 兜底；QuestionCard 语音不支持禁用 |
| 业务规则可溯源 | ✅ | BR 全对照 Business.md：学习-BR-04（混合比）/05（幂等）/39/40/41/42；题库-BR-18/19/20；无臆造 |
| 域边界 | ✅ | SessionResultAggregator 归 Learning 域（Oracle 评审闭环）；GetNextQuestionService 仍 Bank Callee |
| 生成物纪律 | ✅ | 未手写 .g.cs/Controller/DI/gql；mock.g.ts 经 `pnpm gen-mock` 正规管线同步 |

## 六、测试情况

### 自动化测试（主 Agent 实施后复跑，2026-09-24）

| 模块 | 用例数 | 通过 | 失败 | 跳过 | 说明 |
|------|--------|------|------|------|------|
| 后端全套件 `dotnet test tests/XiaoShuTong.Tests/` | 369 | **367** | **0** | 2 | 较 V0.4.0 基线 360 净增 +7：混合比 1 + EndedAt 负例 1 + EndStudy 5 |
| 前端 `pnpm typecheck`（tsc --noEmit） | - | **exit 0** | - | - | 0 错误 |

**新增用例明细**：

| 用例 | 覆盖 |
|------|------|
| `GetNextQuestionServiceTests.ExecuteAsync_MixedRatio_InterleavesSevenThree` | 学习-BR-04 混合比：14 复习 + 6 新题逐题取，断言比例 14:6 + 无连续 ≥4 同池题 + 第 21 题耗尽 |
| `GetSessionQuestionServiceTests.ExecuteAsync_EndedSession_ReturnsSessionEnded` | 学习-BR-05 语义配合：EndedAt 已置 → SESSION_ENDED（3003） |
| `EndStudySessionServiceTests.ExecuteAsync_FirstEnd_WritesThreeFields` | 主流程：写 EndedAt/CorrectCount(2)/TotalTimeMs(3500) + 实体回读 |
| `EndStudySessionServiceTests.ExecuteAsync_EmptySession_WritesEndedAtOnly` | BR-40 空会话：EndedAt 写、计数 0 |
| `EndStudySessionServiceTests.ExecuteAsync_AlreadyEnded_ReturnsCurrentValues` | 幂等：二次调用回读原值不覆盖 |
| `EndStudySessionServiceTests.ExecuteAsync_OthersSession_ReturnsSessionNotFound` | BR-39 负例：他人会话 → 3001 |
| `EndStudySessionServiceTests.ExecuteAsync_AfterEnd_CreateStudySessionCreatesNew` | BR-05 语义：结束后同任务新建会话（幂等失效） |

**既有测试回归**：BR-20 到期复习优先、BR-18 题集耗尽、GetSessionResult 聚合器抽取后断言不变——全绿。

### 遗留缺陷

无阻断性缺陷；待改进项见 §八。

## 七、ADR 执行情况

| ADR | 决策内容 | 是否落实 | 偏差 |
|-----|---------|---------|------|
| ADR-009 | 决策一：分池抽比 + 7:3 混合（Oracle 修订动态池选择） | ✅ | Oracle 评审修订窗口交错→按已答比例（方案正文已更新，ADR 正文不改——决策意图一致，实现方式更优） |
| ADR-009 | 决策二：EndStudySessionService 幂等 + 聚合器 | ✅ | SessionResultAggregator 归 Learning 域（ADR"私有静态"勘误为 internal，方案 T3 已注） |
| ADR-009 | 决策三：触发时机前端主动 + 题集耗尽兜底 | ✅ | 前端三处调用 endStudy；无 Job |
| ADR-009 | 决策四：StripAnswerFromContent | ✅ | 白名单剥字段落地 |
| ADR-009 | 决策五：范围裁剪 + Mic 已有决策 | ✅ | O4/O3 swipe 后置；Mic 走 §6.1 |
| ADR-009 | 状态：活跃（Oracle 评审闭环后转活跃） | ✅ | `docs/adr/README.md` 登记 🟢 活跃；正文未改动（3002 笔误以方案勘误为依据） |

## 八、待改进项

| # | 项 | 描述 | 严重度 | 对策 |
|:-:|----|------|:---:|------|
| 1 | **真实后端端到端联调未跑** | 验证止于 mock 模式 typecheck 级 + 后端 Contract 测试；createSession→getQuestion→submitAttempt→endStudy→sessionResult 全链路未连真实后端浏览器验证 | 中（接入风险） | 下一迭代优先：起 WebApi + WebH5 联调四路径（新学/复习/耗尽/错误） |
| 2 | **O4 连线组件后置** | 题库 O4 仅 4 题（示例/历史/地理），无成规模数据；QuestionCard 对 O4 仅展示 question-only | 低（数据不足） | 题库 O4 成规模后迭代（手写连线组件） |
| 3 | **O3 swipe 手势后置** | 规范要求 true_false 用 swipe，本迭代用对/错双键替代 | 低（交互降级） | 后续经 framer-motion 补 swipe |
| 4 | **BR-24 TaskAssignments.Progress 联动** | 任务完成进度无写入方（SubmitAttemptService L226 桩注释保持） | 低（延期） | V0.6.0+ 迭代（SubmitAttempt 推进 Progress） |
| 5 | **题库 R4 answer 语义验证** | StripAnswerFromContent 保留 R4 answer=卡片正文（schema 验证通过）；真实题库数据待入库后抽查 | 信息 | 题库导入时验证 R4 answer 字段 |
| 6 | **会话超时 Job 未实施** | 触发时机明确无 Job（前端主动 + 题集耗尽兜底）；悬挂会话靠 BR-05 续做 | 信息 | 后续评估 24h 超时扫描 |
| 7 | **混合比 Config 化** | ReviewRatio=0.7 硬编码常量 | 信息 | 有运营需求时切 Config/会话入参 |

## 九、审核结论

- **是否可进入下一阶段：是** —— 367/0/2 全套件 + typecheck 0 错误 + 生成链路全通 + 无阻断项；可 push（不打 tag，tag 纪律另循，V0.5.0 次版本 tag 需征求用户同意）。
- **遗留问题是否阻断：否** —— §八 1-7 为低/信息级（接入风险/延期项/交互降级），均不阻断。
- **下一步建议**：
  1. **真实后端端到端联调**（优先，沿用 V0.4.0 §九 建议 1）——起 WebApi + WebH5 跑通四路径。
  2. **BR-24 TaskAssignments.Progress 联动**立项（V0.6.0+：SubmitAttempt 推进 Progress + 任务完成闭环）。
  3. **O4 连线 / O3 swipe**（题库数据成规模后）。
  4. **题库导入管线补写 Hint**（存量 Questions.Hint 空 → 引导分支空线索兜底已生效）。

## 变更记录（本文件）

| 日期 | 版本 | 变更内容 | 关联ADR |
|------|------|---------|---------|
| 2026-09-24 | V0.5.0 | 初始版本：会话收尾与题型扩展审核报告（总评通过；后端 367/0/2 + 前端 typecheck 0 错误 + 生成链路全通；遗留项为接入风险/延期项不阻断） | ADR-009 |
