---
title: 战力榜接线 - 审核报告
version: V0.6.5
summary: 战力榜 streak/volume/pkWins 单指标透出（RankingItemDto + myRanking）+ rank.tsx power tab 接真实 Combat 接口 + 种子快照补齐
date: 2026-09-25
status: 通过
dev_version: V0.6.5
auditor: Sisyphus（证据审计）
---

# 战力榜接线 - 审核报告（V0.6.5）

## 一、审核范围

- 开发版本：V0.6.5（战力榜接线——V0.6.4 审核报告 §八 遗留核心项）
- 开发方案：V0.6.4 方案关键决策 ③「战力榜接线另立」的执行（对称模式：RankSnapshots 对应 MetricType 单指标直读，Value 求和口径不变）
- 审核日期：2026-09-25
- 审核方式：主 Agent 直接实施 + 证据核验——后端 `dotnet test` 全套件 401/0/2、`pnpm typecheck` 0 错误、契约刷新产物核验
- 审计对象文件清单：

| 层 | 文件 | 任务 |
|----|------|------|
| 后端·榜单 | `src/XiaoShuTong.Domain/Services/Rank/GetRankingsService.cs`（RankingItemDto + BuildRankingAsync 增 Streak/Volume/PkWins 单指标）、`GetMyRankingService.cs`（ResDto + RankOfUserAsync 同增） | 后端 |
| 测试 | `tests/XiaoShuTong.Tests/Rank/GetRankingsServiceTests.cs`（+Combat 单指标/缺失默认 0）、`GetMyRankingServiceTests.cs`（+Combat 单指标） | 后端 |
| 种子 | `src/XiaoShuTong.Domain/XiaoShuTongDomainInitializer.data.cs`（5.4 战力快照直插 3 用户 × Streak/Volume/PkWins） | 数据 |
| 前端 | `src/XiaoShuTong.WebH5/src/routes/student/rank.tsx`（power tab 删 mock 接 rankings_Execute(Combat)，统一数据流 + 双榜共享受控） | 前端 |
| 数据 | `src/XiaoShuTong.WebH5/src/mock/data.ts`（rankings/myRanking 补 streak/volume/pkWins） | 前端 |
| 契约 | `schema.graphql` + `ts-client.g.ts` + `ts-client.mock.g.ts`（buildSchema.ps1 + gen-mock 刷新） | 契约 |
| 文档 | `docs/V0.6.5-战力榜接线-审核报告.md`、`docs/变更记录.md` | 文档 |

## 二、审核结论

**总评：通过**

V0.6.4 审核报告 §八 战力榜遗留项闭环：

1. **后端单指标透出**：`RankingItemDto`/`GetMyRankingResDto` 新增 `Streak`/`Volume`/`PkWins`（RankSnapshots 对应 MetricType 冻结值直读，非求和；`Value` 保持 streak+volume+pk_wins 求和口径 BR-11 不变）。
2. **前端接线**：rank.tsx power tab 删 `MOCK_POWER_RANKING` 硬编码，接 `rankings_Execute(metric='Combat')` + `myRanking_Execute(metric='Combat')`，展示"连续 X 天 · 背诵 Y 篇 · PK胜 Z 次"；统一数据流（Combat/Performance 双榜共用一个 fetchBoard，按 activeTab 切换 metric）。
3. **数据就绪**：联调种子直插战力快照（3 用户 × Streak/Volume/PkWins），mock 数据同步补字段。

**证据**：
- 后端全套件 `dotnet test`：**401 通过 / 0 失败 / 2 跳过**（基线 398 + 3：Combat 单指标 ×2 + MyRanking Combat ×1）
- 前端 `pnpm typecheck`：**0 错误**
- 契约核验：ts-client.g.ts myRanking_Execute/rankings_Execute selection 含 streak/volume/pkWins（L131/L140），GetMyRankingResDto（L826-828）/RankingItemDto（L1322-1324）均有新字段，**无破坏性变更**（仅增字段）
- `dotnet build` Domain 0 错误

## 三、需求符合度（DoD）

| DoD | 状态 | 证据 |
|----|------|------|
| RankingItemDto.Streak/Volume/PkWins 用例（快照单指标/缺失默认 0） | ✅ | ~GetRankingsServiceTests + ~GetMyRankingServiceTests 全绿 |
| rank.tsx power tab 接 rankings(Combat) + 单指标 + typecheck 0 | ✅ | pnpm typecheck 0 错误 |
| mock rankings 补新字段 | ✅ | data.ts 三处补齐 |
| 联调种子 RankSnapshots 战力快照 | ✅ | initializer 5.4 直插 9 行 |
| 全套件 398/0/2 不降 | ✅ | 401 通过 / 0 失败 / 2 跳过（净增 3） |
| 契约向后兼容 | ✅ | 仅增字段，grep + typecheck 验证 |

## 四、关键决策

| # | 决策 | 说明 |
|---|------|------|
| ① | 单指标口径 | 战力榜 streak/volume/pkWins = RankSnapshots 对应 MetricType 冻结值直读（同 V0.6.4 战绩榜 Accuracy/Mastery 模式）；Value 求和口径 BR-11 不变 |
| ② | 前端统一数据流 | fetchBoard(metric) 按 activeTab 切换 Combat/Performance，两榜共用渲染骨架（RankEnabled 门控仅约束 Performance，BR-02/04） |
| ③ | PkWins 切片 07 未实施 | FreezeJob 桩 0（种子直插 0），前端 pkWins>0 才展示"PK胜 X 次" |

## 五、合规核查

- ✅ 未读生成源码；未手写 Controller/DI/gql；未改 Value 求和口径；契约仅增字段向后兼容
- ✅ 测试种子显式 UId；全套件与 typecheck 双绿；未提交 git（commit 待用户指令/交接）

## 六、验证命令回放

```
dotnet build src/XiaoShuTong.Domain/XiaoShuTong.Domain.csproj   # 0 错误
dotnet test tests/XiaoShuTong.Tests/                            # 401 通过 / 0 失败 / 2 跳过
.\buildSchema.ps1                                                # schema + ts-client.g.ts 含 streak/volume/pkWins
pnpm gen-mock && pnpm typecheck                                  # 0 错误
```