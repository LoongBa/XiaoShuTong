---
title: 掌握度透出与走查缺口复核 - 审核报告
version: V0.6.2
summary: 走查附录 4 缺口复核（①②③ 已闭环实证）+ masteryLevel 契约透出（HistoryAccuracy 近 20 次正确率）+ LocalJudgmentEngine 死代码清理
date: 2026-09-25
status: 通过
dev_version: V0.6.2
auditor: Sisyphus（证据审计）
---

# 掌握度透出与走查缺口复核 - 审核报告（V0.6.2）

## 一、审核范围

- 开发版本：V0.6.2（掌握度透出与走查缺口复核）
- 开发方案：`docs/V0.6.2-掌握度透出与走查缺口复核-开发方案.md`（Oracle 评审附条件通过 2 条件 + 1 建议闭环后版本，T1-T5）
- 架构决策：ADR-003（判题契约五键）/ ADR-008（判题元数据去桩）关联参考；本版本为契约扩展非新决策
- 缺口来源：`docs/1-需求分析/V1/背记场景模拟走查-历史.md` 附录"本次模拟暴露的真实缺口"（2026-09-23 时点快照）
- 审核日期：2026-09-25
- 审核方式：主 Agent 直接实施 + 证据核验——后端 `dotnet test` 394/0/2、契约四文件 grep 实证、走查附录复核标注
- 审计对象文件清单：

| 层 | 文件 | 任务 |
|----|------|------|
| 后端·契约 | `src/XiaoShuTong.Domain/Services/Learning/SubmitAttemptService.cs` | T1（ResDto.HistoryAccuracy + 四处返回透出）+ T2（L23 注释整行审查） |
| 后端·清理 | `src/XiaoShuTong.Domain/Tools/LocalJudgmentEngine.cs`（删除） | T2（死代码清理） |
| 契约·生成物 | `src/XiaoShuTong.WebApi/schema.graphql` + `.TKWF/GraphQL_Api.md` + `src/XiaoShuTong.WebH5/src/gql/ts-client.g.ts` + `ts-client.mock.g.ts` | T1（buildSchema.ps1 + gen-mock 全链路刷新） |
| 业务规则 | `.TKWF/Business.md` + `.TKWF/LOG.md` | T3（BR-25 透出口径增订 + 避坑指南） |
| 后端·测试 | `tests/XiaoShuTong.Tests/Learning/SubmitAttemptServiceTests.cs` | T4（+5 用例：加权均值/新题中性/Full 存量/Play 存量/收敛 1.0） |
| 文档 | 走查文档附录 + `docs/变更记录.md` | T3（缺口复核标注）+ T5（变更记录） |

## 二、审核结论

**总评：通过**

V0.6.2 无遗留阻断项。后端证据完整：全套件 **394 通过 / 0 失败 / 2 跳过**（基线 389 净增 +5，T4 补充用例全绿）、`dotnet build` 0 错误。**契约四文件同步实证通过**：`schema.graphql` SubmitAttemptResDto 含 `historyAccuracy: Float!`（末尾）、`GraphQL_Api.md` L526 契约表追加字段、`ts-client.g.ts` 接口含 `historyAccuracy: number`、`ts-client.mock.g.ts` Schema 含 `historyAccuracy: { kind: "number" }`——向后兼容新增，WebH5/AdminWasm 既有调用不消费即可，零破坏。

**Oracle 评审闭环达标**：方案附条件通过 2 条件 + 1 建议全部闭环——条件 1（T4 验收基线 383→389 笔误修正）、条件 2（T3 增补 Business.md BR-25 透出口径增订，对齐 V0.6.1 BR-24 范式）、建议（T2 L23 注释整行审查——LocalJudgmentEngine + TaskAssignments 切片桩字样均已过时，一并改写）。

**实施中发现第 4 返回路径（方案 T1 原只列 3 处）**：幂等命中路径 `BuildFromExistingAsync`（L501）遗漏 HistoryAccuracy 透出——第 2 次起同答案提交命中 BR-27 幂等键直接返回既有结果，该路径无新计算但必须透出存量值。已补 `state?.HistoryAccuracy ?? 0.8`（与 Full/Play 口径一致），测试 T4-5 收敛用例实证此路径（4 变体答案避开幂等，最终断言 1.0）。此发现记入 LOG.md 避坑指南。

## 三、需求符合度

### 走查附录 4 缺口复核结论（核心交付）

| # | 走查声明（2026-09-23） | 复核结论 | git/代码证据 |
|:-:|------------------------|---------|-------------|
| 1 | JudgingEngineService 未接入 SubmitAttempt | **已闭环** | `SubmitAttemptService` L101-110 `User.Use<JudgingEngineService>().JudgeAsync()`；commit `d3c1a56` |
| 2 | SubmitAttemptResDto 缺 needsGuidance | **已闭环** | ResDto L590 `NeedsGuidance` + L239 赋值；commit `93f9f78`；GraphQL_Api.md L526 契约含 |
| 3 | 缺 attemptCount/maxAttempts | **已闭环** | ResDto L594/597 + L238 达上限 showAnswer 兜底；commit `93f9f78`/`39ebf50` |
| 4 | Verify 缺扎实度细粒度（masteryLevel） | **本版闭环** | `SubmitAttemptResDto.HistoryAccuracy` 透出（近 20 次正确率 0~1，PRD L187 数据基础） |

### 方案 §一 目标 3 项 → 实施

| 目标 | 实现状态 | 依据 |
|------|---------|------|
| 1. 走查缺口复核闭环（文档债） | ✅ 完成 | 走查附录标注复核结论（①②③ 已闭环 + ④ 由本版闭环）；变更记录 V0.6.2 条目 |
| 2. masteryLevel 契约透出（唯一真实缺口） | ✅ 完成 | ResDto.HistoryAccuracy 四处返回透出；契约四文件全链路刷新 |
| 3. LocalJudgmentEngine 死代码清理 | ✅ 完成 | 文件删除；grep 全仓 0 命中（除变更记录）；L23 注释整行审查 |

### T1-T5 逐任务对照表

| 任务 | 验收条件（方案 §六） | 实现状态 | 依据 |
|------|--------------------|---------|------|
| T1 契约透出 | build 0 错误 + schema.graphql 含 historyAccuracy + GraphQL_Api.md 追加 | ✅ | ResDto 末尾追加字段（注释含 PRD 溯源）；四处返回透出（正常/幂等/Full/Play）；schema.graphql `historyAccuracy: Float!` + GraphQL_Api.md L526 + ts-client.g.ts + ts-client.mock.g.ts 四文件 grep 实证 |
| T2 清理 | build 0 错误 + grep LocalJudgmentEngine 0 命中 | ✅ | LocalJudgmentEngine.cs 删除；L23 注释整行审查（"跨模块（本切片桩）：判题服务（LocalJudgmentEngine）、TaskAssignments（切片 04）"→ 改为"判题经 JudgingEngineService（d3c1a56 接入，替换 LocalJudgmentEngine 桩）；TaskAssignments 进度联动（BR-24，V0.6.0 落地）"） |
| T3 文档复核 | 附录标注到位 + 变更记录有 V0.6.2 条目 + BR-25 语义行不变透出口径追加 + LOG.md 有记录 | ✅ | 走查附录复核注记；变更记录 V0.6.2 条目；BR-25 行追加"（V0.6.2 透出口径……）"不覆盖语义；LOG.md 2026-09-25 条目（含第 4 路径避坑指南） |
| T4 测试 | 5 用例全绿 + 基线不降 | ✅ | 加权均值 0.5 / 新题中性 0.8 / Full 存量 0.6 / Play 存量 0.6 / 收敛 1.0——44/44 全绿，全套件 394/0/2 |
| T5 文档收口 | 审核报告入档 + 变更记录一致 | ✅ | 本报告 + 变更记录 V0.6.2 条目 |

## 四、架构符合度

| 架构约束（方案 §五 / ADR） | 遵守情况 | 说明 |
|---------------------------|---------|------|
| 契约非破坏性（决策②） | ✅ | ResDto 加字段是向后兼容新增（record init 属性、GraphQL 可选字段、ts-client 类型扩展）；既有调用不消费即零影响；ADR-008"判题契约本体不动"针对去桩不冲突（本版是扩展） |
| 透出原始 HistoryAccuracy（决策①，用户裁决） | ✅ | 零新增计算（MemoryStates.HistoryAccuracy 已存库、CalcHistoryAccuracyAsync 已实现）；前端可直接映射"掌握 70%"展示语义；系数 `Clamp(Acc/0.8,0.5,1.5)` 留后端调度内部 |
| HistoryAccuracy 口径一致性（决策④） | ✅ | 正常路径 `state==null?0.8:newAccuracy`（含本次最新值）；幂等/Full/Play `state?.HistoryAccuracy??0.8`（存量值）；新题首答中性 0.8 与 L175 间隔计算口径一致 |
| 域边界 | ✅ | 无新增跨域写；无新 Service/Controller；仅契约字段扩展 + 死代码清理 |
| 生成物纪律 | ✅ | 契约经 buildSchema.ps1（导出 schema + gen-ts-client）+ gen-mock 全链路刷新；未手写 .g.cs/Dto/DataService/Controller |
| POCO 管控 | ✅ | 无新建 POCO；仅 Dto 字段追加 |

## 五、代码质量

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 类型安全 | ✅ | `double HistoryAccuracy` 非空标量；无 as/强转；Math.Round(,4) 口径在服务端 |
| 异常处理 | ✅ | 无新增异常路径；幂等命中 BuildFromExistingAsync 复用既有 null 合并（`state?.HistoryAccuracy ?? 0.8`） |
| 业务规则可溯源 | ✅ | HistoryAccuracy 注释含 PRD L187 溯源；BR-25 增订不覆盖语义；走查缺口④映射到契约字段 |
| 生成物纪律 | ✅ | 契约四文件全由管线生成（buildSchema.ps1 / gen-mock），无手改 |
| 幂等/路径完整性 | ✅ | **实施发现第 4 返回路径**（幂等命中）并补全——所有 return 点均透出，无遗漏 |
| 死代码清理 | ✅ | LocalJudgmentEngine 0 调用者实证后删除；KeywordGroup 独立文件保留（JudgingEngineService 消费，Oracle 实证） |

## 六、测试情况

### 自动化测试（2026-09-25 复跑）

- 命令：`dotnet test tests/XiaoShuTong.Tests/`（Tier 1.5 SQLite 内存库）
- 结果：**394 通过 / 0 失败 / 2 跳过**（基线 389 +5；2 跳过为 XiaoShuTongServiceTests 模板示例）
- SubmitAttemptServiceTests 分类：44/44 全绿（原 V0.6.1 的 39 用例保持，+5 新增）

### T4 新增用例覆盖矩阵

| 用例 | 场景 | 断言核心 |
|------|------|---------|
| HistoryAccuracy_WeightedAverage | 同题 3 次（Correct/Partial/Wrong） | 第 3 次透出 (1+0.5+0)/3 = 0.5（容差 4 位，Math.Round 口径） |
| HistoryAccuracy_NewQuestionNeutral | 无 MemoryStates 首答 | 中性 0.8（即便本次答对，单次样本不代表性） |
| HistoryAccuracy_FullViewAnswer | hintLevel=Full 早退 | 存量 0.6 透出（不迁移不更新） |
| HistoryAccuracy_PlayScenario | PlayPk 会话 | 存量 0.6 透出（Play 不更新 MemoryStates） |
| HistoryAccuracy_ConvergesToPerfect | 4 变体 Correct 答案（避开幂等） | 第 4 次透出 1.0（均值上限；同答案会命中 BR-27 幂等键） |

## 七、遗留项（不阻断）

| # | 遗留项 | 状态 |
|:-:|--------|------|
| 1 | **前端消费 historyAccuracy**（家长报告/结果页/战绩榜掌握度展示） | 字段已透出向后兼容，前端接线为后续项（UI 文档-v2 掌握度百分比有独立聚合口径） |
| 2 | **masteryLevel 系数化透出**（`Clamp(Acc/0.8,0.5,1.5)` 供前端换算） | 本次透出原始值；系数是后端间隔调度内部语义，前端可自行换算，非必须 |
| 3 | **掌握度等级枚举化**（四阶水平柱外细分） | PRD 无规划，不引入 |
| 4 | 真实后端端到端联调 | 沿用历迭代优先建议，另立 |

## 八、结论

V0.6.2 达成 DoD 全部验收项：

- [x] `dotnet build` 0 错误（Domain + Tests）
- [x] 全套件 **394/0/2**（基线 389 +5，不降反增）
- [x] T4 新增 5 用例全绿
- [x] `schema.graphql` / `ts-client.g.ts` / `ts-client.mock.g.ts` 含 `historyAccuracy`（grep 实证）
- [x] `GraphQL_Api.md` L526 契约表追加 `historyAccuracy` 字段
- [x] `LocalJudgmentEngine` 全仓 0 命中（grep 实证，变更记录除外）
- [x] L23 注释整行审查完成（LocalJudgmentEngine + TaskAssignments 切片桩均不再引用）
- [x] 走查文档附录复核标注到位（①②③ 已闭环 + ④ 由本版闭环）
- [x] Business.md BR-25 透出口径已增订；LOG.md 已记录
- [x] 变更记录 V0.6.2 条目已追加；工作区基线 V0.6.1 已提交（389/0/2）后编码

**总评：通过**。走查附录 4 缺口中 3 项实证已闭环（git 铁证），唯一真实缺口 masteryLevel 以 `HistoryAccuracy` 契约透出闭环（零新增计算、四路径完整、契约四文件同步），死代码清理与文档债复核一并完成。可进入 V0.6.2 提交推送流程。
