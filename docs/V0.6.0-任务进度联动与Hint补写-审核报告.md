---
title: 任务进度联动与Hint补写 - 审核报告
version: V0.6.0
summary: 任务进度联动与Hint补写（BR-24 TaskAssignments.Progress + Hint 补写 join 卡片 + O4/O3 前端）
date: 2026-09-24
status: 通过
dev_version: V0.6.0
auditor: Sisyphus（证据审计）
---

# 任务进度联动与Hint补写 - 审核报告（V0.6.0）

## 一、审核范围

- 开发版本：V0.6.0（任务进度联动与Hint补写）
- 开发方案：`docs/V0.6.0-任务进度联动与Hint补写-开发方案.md`（Oracle 评审闭环后版本，8 节 + 变更记录，T1-T9）
- 架构决策：`docs/adr/ADR-010-任务进度联动契约.md`（活跃；决策二"实现"行有 Oracle 勘误注，Attempts 无 AttemptCount 字段）
- 审核日期：2026-09-24
- 审核方式：主 Agent 直接实施 + 证据核验——后端 `dotnet test` 383/0/2、前端 `pnpm typecheck` 0 错误、生成链路 SG1→SG2→SG3 全通（importBankJson_Execute 契约）
- 审计对象文件清单：

| 层 | 文件 | 任务 |
|----|------|------|
| 后端 | `src/XiaoShuTong.Domain/Services/Learning/SubmitAttemptService.cs` | T1（SyncTaskProgressAsync） |
| 后端 | `.TKWF/Business.md` | T2（BR-24 口径/状态机/C-18/C-20 修订） |
| 后端 | `src/XiaoShuTong.Domain/Services/Bank/ImportBankJsonService.cs`（新增） | T4（JSON 导入 + Hint join + Content card 镜像） |
| 后端 | `src/XiaoShuTong.Domain/Services/Bank/BankHintBackfillJob.cs`（新增） | T5（存量回填） |
| 后端 | `tests/XiaoShuTong.Tests/Learning/SubmitAttemptServiceTests.cs`（BR-24 7 用例）+ `Bank/ImportBankJsonServiceTests.cs`（新增 6+3 用例） | T3/T6 |
| 前端 | `src/XiaoShuTong.WebH5/src/components/QuestionCard.tsx` + `lib/question-content.ts` | T7（O4 连线 + O3 swipe + pairs 解析） |
| 前端 | `src/XiaoShuTong.WebH5/src/mock/data.ts` | T8（O4 pairs 样例） |
| 生成产物 | `schema.graphql` / `ts-client.g.ts` / `ts-client.mock.g.ts` / `.TKWF/` 活态文档 | SG 链路自动刷新 + buildSchema/gen-mock |

## 二、审核结论

**总评：通过**

V0.6.0 无遗留阻断项。后端证据完整：全套件 **383 通过 / 0 失败 / 2 跳过**（基线 367 净增 +16：BR-24 7 用例 + JSON 导入 6 + 回填 3）、生成链路 SG1→SG2→SG3 全通（`importBankJson_Execute` 入 schema.graphql + ts-client.g.ts + ts-client.mock.g.ts）、Business.md 增量修订在案（BR-24 口径/状态机/C-18 例外/C-20 补列）。前端 typecheck 0 错误，mock 模式 O4 连线组件 + O3 swipe 可用。

**Oracle 评审闭环达标**：方案附条件通过 12 条意见（1 高 / 5 中 / 3 低 / 3 信息）全部闭环——#1（AttemptCount 字段不存在→GroupBy+Count 勘误，同步 ADR-010"后果"节勘误注）、#2（跨会话查询两步路径）、#3（回填匹配算法）、#4（Job 路径改 Services/Bank）、#5（JsonPath 生产首选）、#6（BR-23 措辞调和）、#7（O4 JSON 格式定案）、#9（导入幂等键 QuestionId）已按反馈修正。

## 三、需求符合度

### 方案 §一 目标 3 项 → 实施

| 目标 | 实现状态 | 依据 |
|------|---------|------|
| 1. BR-24 TaskAssignments.Progress 联动（ADR-010 决策一~五） | ✅ 完成 | SubmitAttemptService SyncTaskProgressAsync（重算/跨会话/InProgress/Completed/三字段接线/豁免路径）；BR-24 7 用例全绿（推进/完成/个人会话豁免/Full 豁免/幂等/达上限消费） |
| 2. Hint 补写（ADR-010 决策六） | ✅ 完成 | ImportBankJsonService（knowledge_card_id join 卡片记忆钩子 + Content card 镜像 + upsert 幂等）+ BankHintBackfillJob（存量回填精确/包含匹配 + 未命中留空）；JSON 导入 6 + 回填 3 用例全绿 |
| 3. O4/O3 前端 | ✅ 完成 | QuestionCard O4 连线组件（点击配对 + JSON {"pairs":{...}} 提交）+ O3 swipe（framer-motion 左滑对/右滑错，双键降级）；question-content.ts pairs 解析；mock O4 样例 |

### T1-T9 逐任务对照表

| 任务 | 验收条件（方案 §六） | 实现状态 | 依据 |
|------|--------------------|---------|------|
| T1 SyncTaskProgress | Progress 推进/Completed/豁免断言 | ✅ | SubmitAttemptService L255-317（新增方法）；7 用例含 TaskId/null/Full/幂等/达上限 |
| T2 Business.md | BR-24 口径 + 状态机 + C-18/C-20 | ✅ | BR-24 行补全判定口径；§3.3 Overdue→Completed 边注释（BR-23 措辞调和）；C-18 例外 + C-20 补列 |
| T3 BR-24 测试 | 7 用例全绿，基线不降 | ✅ | ExecuteAsync_TaskAttempt_ProgressAdvances / TaskAllConsumed / PersonalSession / FullHint / IdempotentHit / WrongUnderLimit / WrongAtLimit |
| T4 JSON 导入 | Hint join + Content card 镜像 + 幂等 | ✅ | ImportBankJsonService（内联 DTO + [GenerateController]）；6 用例（权限/Hint join/无卡片/upsert/JsonPath） |
| T5 回填 Job | Hint 空→补齐/未命中留空 | ✅ | BankHintBackfillJob（Services/Bank 域，对齐 BankContentPreprocessJob）；3 用例 |
| T6 Hint 测试 | 用例全绿 | ✅ | ImportBankJsonServiceTests.cs（6+3 用例，383/0/2 内） |
| T7 O4/O3 | O4 连线 + O3 swipe + typecheck | ✅ | QuestionCard O4 配对组件（handleLeftClick/handleRightClick/submitPairs）+ O3 motion.div drag；typecheck 0 错误 |
| T8 mock | O4 pairs 样例 | ✅ | data.ts q-8 O4（pairs 4 对：诗句→作者） |
| T9 文档 | 变更记录 + ADR 活跃 + 审核报告 | ✅ | 变更记录 V0.6.0 条目本次追加；ADR-010 已转活跃（Oracle 闭环后）；本报告 |

## 四、架构符合度

| 架构约束（ADR-010 / 方案 §五） | 遵守情况 | 说明 |
|-------------------------------|---------|------|
| ADR-010 决策一：重算式（非增量） | ✅ | consumed.Count / Tasks.QuestionCount × 100，幂等天然 |
| ADR-010 决策二：完成判定 Correct\|count≥MaxAttempts | ✅ | Oracle 勘误：GroupBy(QuestionId).Where(g => g.Any(Result==Correct) \|\| g.Count() >= MaxAttempts)（Attempts 无 AttemptCount 字段） |
| ADR-010 决策三：跨会话聚合 | ✅ | 两步查询（StudySessions.TaskId 会话集 → Attempts.SessionId ∈ 集合），支持 BR-05 续做/BR-23 逾期复习 |
| ADR-010 决策四：状态迁移 | ✅ | Pending→InProgress（首次推进）/Progress≥100→Completed+CompletedAt/Completed 幂等；Overdue→Completed 允许（BR-23 措辞调和注释） |
| ADR-010 决策五：三预留字段 + C-18/C-20 | ✅ | SessionId（最新会话指针)/StartedAt/CompletedAt 接线；Business.md C-18 例外 + C-20 补列 |
| ADR-010 决策六：Hint join 卡片不改 schema | ✅ | knowledge_card_id → 卡片 fields.记忆钩子；question.v1.json 维持 v1.1；Content card 镜像修复（GetKnowledgeCardService 激活） |
| 域边界 | ✅ | SessionResultAggregator 归 Learning（V0.5.0 已定）；BankHintBackfillJob 归 Services/Bank（同域范式）；Progress 归 SubmitAttempt（ADR-009 明确） |
| POCO 管控 | ✅ | ImportBankJsonResDto/ReqDto 同文件内联（Request/响应形状正当）；无冗余 Res POCO |

## 五、代码质量

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 类型安全（前端） | ✅ | QuestionCard O4 pairMap/submitPairs 类型明确；motion.div onDragEnd 类型正确；0 as any/@ts-ignore |
| 异常处理 | ✅ | ParseCards TryAdd（修复 ??= 对不存在 key 先读抛 KeyNotFoundException 缺陷）；FindKnowledgeCardsFile 空安全；回填未命中留空计数不抛 |
| 业务规则可溯源 | ✅ | BR 全对照 Business.md：学习-BR-24/05、任务-BR-05/22/23、C-05/17/18/20；无臆造 |
| 生成物纪律 | ✅ | ImportBankJsonService 经 SG1 生成；schema/ts-client/mock 全经 buildSchema/gen-mock 正规管线（不手改） |
| 并发/幂等 | ✅ | 回填幂等（已回填跳过）；导入幂等（QuestionId upsert）；BR-24 重算天然幂等 |

## 六、测试情况

### 自动化测试（2026-09-24 复跑）

| 模块 | 用例数 | 通过 | 失败 | 跳过 | 说明 |
|------|--------|------|------|------|------|
| 后端全套件 dotnet test tests/XiaoShuTong.Tests/ | 385 | 383 | 0 | 2 | 较 V0.5.0 基线 367 净增 +16：BR-24 7 + JSON 导入 6 + 回填 3 |
| 前端 pnpm typecheck（tsc --noEmit） | - | exit 0 | - | - | 0 错误（buildSchema/gen-mock 同步后复跑） |

**新增用例明细（BR-24）**：
- ExecuteAsync_TaskAttempt_ProgressAdvancesAndInProgress：1 题 Correct → 50% + InProgress + SessionId/StartedAt 接线
- ExecuteAsync_TaskAllConsumed_CompletesAssignment：2 题消费 → 100% + Completed + CompletedAt
- ExecuteAsync_PersonalSession_DoesNotAdvanceProgress：TaskId=null 不创建分配
- ExecuteAsync_FullHint_DoesNotAdvanceProgress：Full 早退不推进
- ExecuteAsync_IdempotentHit_DoesNotReAdvanceProgress：幂等命中不重复推进
- ExecuteAsync_WrongUnderLimit_DoesNotConsume：Wrong 未达上限不计数
- ExecuteAsync_WrongAtLimit_Consumes：达上限（改答案重试）→ 消费 100%

**新增用例明细（Hint）**：ImportBankJson 6（权限x2/Hint join/无卡片/幂等 upsert/JsonPath 缺失）+ Backfill 3（补齐/无映射留空/已有 Hint 不覆盖）。

**测试过程修复（审计记录）**：3 处测试缺陷修正——BR-24 幂等键冲突（同答案重发命中幂等→改答案重试）、EntitySelect/Get 位置参数 ct 命名、Backfill hooks[title] ??= hook 抛 KeyNotFoundException（改 TryAdd）。均为实现/测试耦合问题，非业务缺陷。

## 七、ADR 执行情况

| ADR | 决策内容 | 是否落实 | 偏差 |
|-----|---------|---------|------|
| ADR-010 | 决策一~六（重算/完成判定/跨会话/状态迁移/三字段+C-18/join 卡片） | ✅ | 决策二"实现"行 AttemptCount 字段勘误（ADR"后果"节勘误注，决策语义不变）——Oracle 评审闭环 |
| ADR-010 | 状态：活跃 | ✅ | README 登记 活跃（2026-09-24），正文决策保留 |
| ADR-009 | Progress 归 SubmitAttempt 职责（V0.5.0 排除） | ✅ | 本迭代在 SubmitAttemptService 落地，EndStudySession 未联动 |

## 八、待改进项

| # | 项 | 描述 | 严重度 | 对策 |
|:-:|----|------|:---:|------|
| 1 | 真实后端端到端联调未跑 | 验证止于 mock typecheck + Contract 测试；Progress 跨会话聚合/回填 Job 全链路未连真实后端浏览器验证 | 中（接入风险） | 沿用 V0.4.0/V0.5.0 优先建议，另立联调迭代 |
| 2 | 重算热路径 O(N2) 读模式 | 每次作答全量聚合该任务 attempts，50 题会话内累加 ~1275 次读取 | 低 | 后续增量维护 consumed 集合 + 完成时全量校验（ADR-010"后续待办"已登记，本迭代不改） |
| 3 | 回填匹配精度受限 | txt/AI 草稿存量行无 knowledge_card_id，靠 KnowledgePoints[0] 篇名匹配卡片 title，最佳努力 | 低 | 语文覆盖良好（卡片 title 含篇名）；其余学科 md→JSON 结构化后覆盖（后续项） |
| 4 | O4 题库数据不足 | 真实学科题库 O4 仅历史/地理各 4 题；组件就绪 + mock 验证，真实验证待数据 | 低 | 题库 O4 成规模后验证（pairs JSON 契约已定） |
| 5 | 其他学科背景钩子仅 md | Hint 覆盖率限语文（7-9 年级 130/130 + 小学 67/92） | 信息 | md→JSON 结构化列入后续项 |
| 6 | 导入前置校验 | ImportBankJsonService 无 schema 校验（question.v1.json 语义靠解析容错） | 信息 | 后续接入 schema validator（ADR-006 版本化） |

## 九、审核结论

- 是否可进入下一阶段：是 —— 383/0/2 全套件 + typecheck 0 错误 + 生成链路全通 + Oracle 12 条意见闭环 + 无阻断项；可 push（不打 tag，V0.6.0 tag 需征求用户同意）。
- 遗留问题是否阻断：否 —— 八 1-6 均为接入风险/优化项/数据覆盖限制，不阻断。
- 下一步建议：
  1. 真实后端端到端联调（优先，沿用历迭代建议）——Progress 推进/回填/四态全链路验证。
  2. 重算热路径优化（增量 + 完成时校验）。
  3. 其他学科背景钩子 md→JSON 结构化 + 回填覆盖扩科。
  4. O4 题库成规模后真实验证连线组件。

## 变更记录（本文件）

| 日期 | 版本 | 变更内容 | 关联ADR |
|------|------|---------|---------|
| 2026-09-24 | V0.6.0 | 初始版本：任务进度联动与Hint补写审核报告（总评通过；后端 383/0/2 + 前端 typecheck 0 错误 + Oracle 12 条闭环；遗留项为接入风险/优化项不阻断） | ADR-010 |