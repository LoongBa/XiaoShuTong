---
title: 学生作答流端到端接线 - 审核报告
version: V0.4.0
summary: 学生作答流端到端接线（G-1 getSessionQuestion 契约暴露 + G-2 判题元数据去桩 + task/result 前端四态接线 + mock 补数）
date: 2026-09-24
status: 通过
dev_version: V0.4.0
auditor: Sisyphus（证据审计）
---

# 学生作答流端到端接线 - 审核报告（V0.4.0）

## 一、审核范围

- 开发版本：V0.4.0（学生作答流端到端接线）
- 开发方案：`docs/V0.4.0-学生作答流端到端接线-开发方案.md`（Oracle 评审闭环后版本，8 节 + 变更记录，T1-T11）
- 架构决策：`docs/adr/ADR-008-会话出题契约暴露与判题元数据去桩.md`（活跃）
- 审核日期：2026-09-24
- 审核方式：只读证据审计——审计对象文件逐一核验（不读生成源码，生成物仅 Grep 字段出现）+ 主控已验证证据直接引用（后端 `dotnet test` 360/0/2、前端 `pnpm typecheck` exit 0）
- 审计对象文件清单：

| 层 | 文件 | 任务 |
|----|------|------|
| 后端 | `src/XiaoShuTong.Domain/Entities/Bank/Questions.cs` | T1（Hint 列） |
| 后端 | `src/XiaoShuTong.Domain/Services/Learning/GetSessionQuestionService.cs` | T2（BR-06 + 转发） |
| 后端 | `src/XiaoShuTong.Domain/Tools/QuestionMetaProvider.cs` + `LearningQuestionRegistry.cs` | T3（去桩封装 / 注册表降级） |
| 后端 | 5 消费点：SubmitAttemptService / GetHintService / GetSessionResultService / GetWrongQuestionsService / KnowledgeMasteryAggregationJob | T3 |
| 后端 | `tests/XiaoShuTong.Tests/Learning/GetSessionQuestionServiceTests.cs` + 5 个既有测试切真实 InMemory 数据 | T4 |
| 前端 | `src/XiaoShuTong.WebH5/src/routes/student/study/task.tsx` | T5/T6/T7 |
| 前端 | `src/XiaoShuTong.WebH5/src/routes/student/study/result.tsx` | T8 |
| 前端 | `src/XiaoShuTong.WebH5/src/routes/student/study/review.tsx` + `src/store/appStore.ts` | T9 |
| 前端 | `src/XiaoShuTong.WebH5/src/mock/data.ts` + `src/lib/memory-state.ts` | T10 / 新增辅助 |
| 生成产物 | `src/XiaoShuTong.WebApi/schema.graphql` / `src/XiaoShuTong.WebH5/src/gql/ts-client.g.ts` / `.TKWF/Domain_Api.md` + `GraphQL_Api.md` + `DOMAIN_MAP.md` | 自动刷新核验（Grep 字段出现） |

## 二、审核结论

**总评：通过（附条件项已闭环）**

本迭代与上一迭代（判题契约扩展，总评附条件通过）不同：**无遗留阻断项**，mock 轮转限制等为已知机制约束非缺陷。后端证据完整（360/0/2 全套件 + 生成链路 SG1→SG2→SG3 全通 + 注册表零业务调用），前端 typecheck 0 错误、mock 模式四态 + 耗尽 + 空/错误场景可见。

> **审计闭环说明（2026-09-24 修订）**：初稿总评为"附条件通过"，审计发现 3 处真实后端契约对齐问题（§八 1/2/3：memory-state 映射错配、hint difficultySlot 语义错配、mock blockedPoints 缺 state）。**三项已全部修复并通过 typecheck 0 错误复核**（§八 1/2/3/4 状态=已修复）：`memory-state.ts` 增 PascalCase→MemoryState 映射、task.tsx difficultySlot 改 S1/S2/S3（BR-30 对齐）、mock blockedPoints 补 state + 连带修复 result.tsx reason 判定。遗留项均为低/信息级登记延期（§八 5-11），不阻断交付。**总评上调为通过**。真实后端端到端联调仍为下一迭代优先建议（非阻断）。

## 三、需求符合度

### 方案 §一 目标 4 项 → 实施

| 目标 | 实现状态 | 依据 |
|------|---------|------|
| 1. G-1 出题契约暴露（ADR-008 决策一） | ✅ 完成 | `sessionQuestion_Execute` 契约全链路出现（schema.graphql:67 / ts-client.g.ts:38,145,1886 / `.TKWF/Domain_Api.md`:313 / `GraphQL_Api.md`:245）；Bank 域 `GetNextQuestionService` 保持 internal Callee 不动 |
| 2. G-2 判题元数据去桩（ADR-008 决策二） | ✅ 完成 | 5 消费点全部切换 `QuestionMetaProvider` 真实读取；`Questions.Hint` 列（Position 15）落库；`LearningQuestionRegistry` Services 目录 0 引用（仅 Tools 自身 + QuestionMetaProvider 注释提及） |
| 3. 前端端到端接线（审核报告 §八 #1/#2 闭环） | ✅ 完成（mock 模式） | task.tsx 会话初始化/四态/hint、result.tsx sessionResult、review 入口传参、appStore 会话三件套；**真实后端端到端未跑（见 §二/§八 8）** |
| 4. mock 补数 | ✅ 完成 | data.ts submitAttemptResDtos 4 条新契约（15 字段）+ getNextQuestionResDtos 6 条 + getHintResDtos 语文文案 + scenarioOverrides 覆盖 error/guidance/answerSheet/degraded/exhausted/empty |

### 缺口 G1-G4 闭环状态（上一迭代遗留 → 本迭代）

| 缺口 | 上一迭代状态 | 本迭代闭环状态 |
|------|-------------|----------------|
| G1 getSessionQuestion 契约暴露 | 缺失（GetNextQuestionService 无 [GenerateController]） | ✅ **闭环**（Learning 薄包装对外暴露，见目标 1） |
| G2 元数据去桩 | 缺失（LearningQuestionRegistry 静态桩被 5 服务消费） | ✅ **闭环**（真实读取 + Hint 落库，见目标 2） |
| G3 70/30 新题/复习混合比 | 未排期 | ⏸️ **延期（不包含项）**，另立决策（方案 §二 明确排除） |
| G4 EndStudySession（EndedAt 写入） | 未排期 | ⏸️ **延期（不包含项）**，另立决策；EndedAt==null 校验随 G-4 落地同步补（GetSessionQuestionService 注释已登记） |

### T1-T11 逐任务对照表

| 任务 | 验收条件（方案 §六） | 实现状态 | 依据（只读核验） |
|------|--------------------|---------|-----------------|
| T1 | Questions 含 Hint 列（Position 15） | ✅ | `Questions.cs:90-92` `[Column(Position = 15, StringLength = 200)] public string? Hint`；`.TKWF/DOMAIN_MAP.md:347`（`Hint \| string? \| Y`）；`ts-client.g.ts` QuestionsDto（`hint: string \| null`） |
| T2 | getSessionQuestion 契约 SG1→SG2→SG3 全链路 + 复用 ResDto | ✅ | 类级 `[GenerateController][AuthorityFilter][Transactional]` internal（L23-27）；BR-06 归属校验 → SessionNotFound 3001（L40-42）；转发 `User.Use<GetNextQuestionService>().ExecuteAsync`（L45-51）；响应 `GetNextQuestionResDto`（L35）+ ReqDto 同文件内联（L59-71） |
| T3 | 注册表零业务调用 + 真实读取单测 | ✅ | QuestionMetaProvider `KeywordsJson` 原样传递（`question.Keywords`，L80）+ `LoadSubjectMapAsync` 全量批量预取（L84-91）+ `GetManyAsync` 批量 IN（L59-71）；5 消费点引用 `QuestionMetaProvider`（SubmitAttempt L70/L94/L224、GetHint L38/L63、GetSessionResult L73、GetWrongQuestions L68、KnowledgeMasteryAggregationJob L56）；Services 目录 Grep `LearningQuestionRegistry` = 0 命中 |
| T4 | GetSessionQuestionServiceTests BR-17/18/19/20 ≥1 + BR-06 负例 + Hint 回归 | ✅ | 6 Facts：BR-06 非本人 3001（L94-106）、BR-06 不存在 3001（L108-118）、BR-17 Forbidden（L120-134）、BR-19 防爬不含答案/关键词（L136-155）、BR-18 题集耗尽空结果（L157-188）、BR-20 到期复习优先（L190-218） |
| T5 | validateSearch 两分支 + createStudySession→sessionUid→首题 + 无参数引导 | ✅ | task.tsx validateSearch（L46-52）+ initSession 归口 useCallback（L98-134）：复习=Assess/新学=Memorize、无参数 → guide、首次取题失败 → error 重试卡、题集耗尽 → 直接 result |
| T6 | useAttemptFlow 四态 + MOCK_QUESTIONS/本地判题删除 + 分母=questionCount | ✅ | handleSubmit 四态分支（L186-238）：celebrate 1.5s 后下一题 / guidance 再试+求助 / answer-sheet 合集标绿标红必进下一题 / error 停留；`fetchNextQuestion`（L152-174）经 `SessionQuestion_ExecuteService`，耗尽 → result；第 x/y 分母 = `questionCount`（L344）；MOCK_QUESTIONS 本地常量已删（mock 数据下沉 data.ts） |
| T7 | hint_Execute 服务端文案 + None→Partial→Full 回填 | ✅（发现 1 处契约错配） | handleHint（L256-272）：`hint_Execute` 服务端 hint + 仅成功升档（防 BR-16 误触发）+ 空 hint 通用鼓励语兜底；**⚠️ difficultySlot 传 'Partial'/'Full' 与后端 BR-30 S1/S2/S3 校验错配（见 §八 2）** |
| T8 | sessionResult_Execute 四字段 + stuckItems 硬编码删除 + 空会话回首页 | ✅（发现 1 处展示缺陷） | result.tsx L46-81 全字段来自服务端；L97-102 blockedPoints 替换 stuckItems 硬编码；L49-53 空会话（BR-40）→ 回首页；**⚠️ reason 判定与 state 展示恒'薄弱'/灰（见 §八 3/4）** |
| T9 | store 三件套 + answers 不进 persist + review 入口传参 | ✅ | appStore `ServerAnswerRecord`（L13-22）替换本地推导；`startSession` 归口（L143-174）；partialize（L370-378）不含会话态（维持不进 persist）；review.tsx `navigate({ search: { reviewQuestionId } })`（L42-48） |
| T10 | data.ts 三表新契约 + 场景轮转/覆盖 | ✅ | submitAttemptResDtos 4 条（Correct/Partial/Wrong 达上限/degraded，L124-173）；getNextQuestionResDtos 6 条含耗尽空结果（L176-214）；getHintResDtos 语文唐诗背景钩子（L217-220）；scenarioOverrides error/guidance/answerSheet/degraded/exhausted/empty（L706-856） |
| T11 | 变更记录 + ADR 转活跃 + 审核报告 + 勾选 | ✅（本报告实施中） | ADR-008 已于 Oracle 评审闭环转活跃（`docs/adr/README.md` 🟢 2026-09-24）；判题契约扩展方案 §十 步骤 0-6 已于上一迭代全勾选（本次无新增勾选项）；变更记录追加见本报告"变更记录"节 |

## 四、架构符合度

| 架构约束（ADR-008 / 方案 §五） | 遵守情况 | 说明 |
|-------------------------------|---------|------|
| ADR-008 决策一：Learning 薄包装 + Bank Callee 保持 internal | ✅ | `GetNextQuestionService` 无 `[GenerateController]` 保持 internal（未改动）；`GetSessionQuestionService` 为 Learning 域对外 RPC 入口，与 CreateStudySession/SubmitAttempt 同域 |
| ADR-008 决策一：响应复用 `GetNextQuestionResDto`（不新建 Res POCO） | ✅ | 薄包装返回类型即 Bank 域既有 ResDto；仅新建同文件内联 ReqDto；实现注释标注"跨域复用：变更需同步 Learning 域"（R4 对策落地） |
| ADR-008 决策二：`Questions`/`Banks` 真实读取 + Hint 落库 | ✅ | QuestionMetaProvider 只读封装真实 DataService；`Questions.Hint` 列新增并经 xCodeGen 重生成契约 |
| ADR-008 决策二：注册表降级测试专用 | ✅ | `LearningQuestionRegistry` 保留（Tools 下，注释更新"注册表降级测试专用"），5 业务消费点零引用 |
| Keywords **原样传递**（Weight/Required/Aliases 保留，Oracle P0 闭环） | ✅ | `QuestionMeta.KeywordsJson = question.Keywords`（JSON 原文）；SubmitAttemptService L98 `Keywords = question.KeywordsJson` 直接传 JudgingEngineService（删除平铺重包）；JudgingEngineService `ParseKeywordGroups` 可直接反序列化 |
| BR-06 会话归属校验（Oracle P0 闭环） | ✅ | 薄包装唯一业务逻辑：`session == null \|\| session.UserId != userId → SessionNotFound(3001)`（GetSessionQuestionService L40-42），域边界职责由 Learning 承担（Bank Callee 无职责承载） |
| 批量预取防 N+1（Oracle #9） | ✅ | `LoadSubjectMapAsync` 一次全量取 Banks 构 `Dictionary<BankId, Subject>`；`GetManyAsync` 一次 IN 查询（GetSessionResult/GetWrongQuestions/KnowledgeMastery 三消费点均走批量） |
| `[Transactional]` 读一致性 | ✅ | GetSessionQuestionService 类级 `[Transactional]`（跨 QuestionsDs+BanksDs+StatesDs+AttemptsDs 一致性读快照，仅读不写无提交副作用） |
| 跨域契约耦合风险登记（R4） | ✅ | 实现注释显式标注跨域复用 Bank.GetNextQuestionResDto；方案 §七 R4 对策一致 |
| POCO 管控（§6.1.1 决策树） | ✅ | 零新建 Res POCO：GetSessionQuestionService 复用 GetNextQuestionResDto、SubmitAttempt 复用 SubmitAttemptResDto（既有 5 字段契约不动）、GetWrongQuestions 复用 WrongQuestionsDto + 计算字段；仅 ReqDto 同文件内联 |

## 五、代码质量

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 类型安全（前端） | ✅ | 手写代码 0 `as any` / `@ts-ignore`（Grep 全 `src/`，唯一命中为 TanStack 生成的 `routeTree.gen.ts`，非手写）；判题反馈四态 `FeedbackState` 判别联合（task.tsx L36-41）穷尽分支 |
| 异常处理（前端） | ✅ | hook 四态降维 + 原始响应透传（postState 徽章 / isDegraded 轻提示）；task.tsx 加载失败/空态/提交失败/下一题失败全有兜底 UI（错误态卡片 + 重试）；result.tsx 结果加载失败 → 错误态 + 回首页；全局错误由 `Tkwf.configure` onUnauthorized/onGlobalError 兜底 |
| 业务规则可溯源 | ✅ | BR 全对照 `.TKWF/Business.md`：学习-BR-06（会话归属）、题库-BR-17/18/19/20（权限/耗尽/防爬/排序）、学习-BR-26/28（题目存在性）、BR-29（提示 ≤20 字 `TruncateHint`）、BR-32/33（required 强制 partial / 同义词组——Keywords 原样传递保障）、BR-42（blockedPoints=✕/△ 题目+知识点）；无臆造规则 |
| POCO 管控 | ✅ | 后端零新建 Res POCO（见 §四）；前端零新建类型膨胀（`ServerAnswerRecord` 对齐服务端契约、`memory-state.ts` 为辅助映射函数） |
| 守卫规则 / 生成物纪律 | ✅ | 未发现手写 `.g.cs`/DataService/Controller/DI 注册；生成物（schema/ts-client/.TKWF）全为 AfterBuild/codegen 自动刷新 |

## 六、测试情况

### 自动化测试（主控已验证，直接引用）

| 模块 | 用例数 | 通过 | 失败 | 跳过 | 说明 |
|------|--------|------|------|------|------|
| 后端全套件 `dotnet test tests/XiaoShuTong.Tests/` | 362 | **360** | **0** | 2 | 较上迭代基线 353 净增 +7；新增 GetSessionQuestionServiceTests 6 用例 + Hint 去桩回归，另有 5 个既有注册表桩测试切换真实 InMemory Questions/Banks 数据（用例数与注册表 Register 调用同步调整，故为净增） |
| 前端 `pnpm typecheck`（tsc --noEmit） | - | **exit 0** | - | - | 0 错误 |

**GetSessionQuestionServiceTests 6 用例明细**（`tests/XiaoShuTong.Tests/Learning/GetSessionQuestionServiceTests.cs`，Tier 1.5 SQLite 内存库，真实 InMemory Questions/Banks/StudySessions/Attempts/MemoryStates 种子）：

| 用例 | 覆盖 BR | 断言要点 |
|------|---------|---------|
| `ExecuteAsync_OthersSession_ReturnsSessionNotFound` | 学习-BR-06（负例） | 他人会话 → `SessionNotFound`(3001)，`Success=false` |
| `ExecuteAsync_NonexistentSession_ReturnsSessionNotFound` | 学习-BR-06（负例） | 会话不存在 → `SessionNotFound`(3001) |
| `ExecuteAsync_PrivateBankNotOwner_ReturnsForbidden` | 题库-BR-17 | 私库非 Owner → `BankErrorCodes.Forbidden` |
| `ExecuteAsync_ValidSession_ReturnsQuestionWithoutAnswer` | 题库-BR-19 | 返回 QuestionId/Type=R1/KnowledgePoint；JSON 序列化断言不含 "answer" / 关键词"以观沧海" |
| `ExecuteAsync_AllAnswered_ReturnsEmpty` | 题库-BR-18 | 会话已答全部题 → `Success=true` + `QuestionId==""`（会话结束信号） |
| `ExecuteAsync_DueReview_PrioritizedOverNew` | 题库-BR-20 | 到期复习（NextReviewAt 过去）优先于新题 |

**Hint 去桩回归**：SubmitAttempt 引导分支 hint 来自真实 `Questions.Hint`（测试种子 `Hint = "首字：以"`，非注册表桩），空/非空各覆盖。

### 前端验证

- `pnpm typecheck` 0 错误（主控已验证）；mock 模式 task.tsx 真实链路（createSession → getSessionQuestion → submitAttempt 四态 → 耗尽 → result）经 scenarioOverrides 场景切换走查（error/guidance/answerSheet/degraded/exhausted 场景数据齐备）。
- **如实记录：无浏览器级端到端验证**——未连真实后端（createStudySession → getSessionQuestion → submitAttempt → sessionResult 全链路），验证止于 mock 模式 typecheck 级。由此暴露的 3 处契约对齐风险见 §八 1/2/3。

### 遗留缺陷

| 缺陷 | 严重度 | 状态 | 对策 |
|------|--------|------|------|
| （无阻断性缺陷） | - | - | 全部遗留项见 §八 待改进项 |

## 七、ADR 执行情况

| ADR | 决策内容 | 是否落实 | 偏差 |
|-----|---------|---------|------|
| ADR-008 | 决策一：Learning 薄包装对外暴露 getSessionQuestion，Bank Callee 保持 internal，响应复用 GetNextQuestionResDto | ✅ | 无（§四 核验） |
| ADR-008 | 决策二：Questions/Banks 真实读取 + Questions.Hint 落库 + 注册表降级测试专用 | ✅ | 无（§四 核验） |
| ADR-008 | 状态：活跃（Oracle 评审闭环后转活跃） | ✅ | `docs/adr/README.md` 状态表已登记 🟢 活跃（2026-09-24）；ADR-008 正文未改动 |
| ADR-008 | 后续待办：G-3 / G-4 另立决策；mock getNextQuestionResDtos 补表 | ✅ | G-3/G-4 延期登记（§三）；mock 补表已落地（T10） |

决策记录闭环：ADR-008 目的/问题/场景/决策/后果五段齐备且与实施一致，无"决策已做但实施背离"情况。

## 八、待改进项

| # | 项 | 描述 | 严重度 | 状态 | 对策 |
|:-:|----|------|:------:|:---:|------|
| 1 | **memory-state 映射错配（真实后端徽章恒灰）** | 新增辅助 `src/lib/memory-state.ts` 仅映射显示符号 `✕/△/○/★` → MemoryState；而真实后端下发 `PreState = preState.ToString()` 为 **PascalCase 枚举名**（NotMastered/Fuzzy/Mastered/Proficient，Enums.cs:74-87 确认）；GetSessionResult `BlockedPointDto.state` 同理。mock 数据用符号（与前端期望一致），掩盖了差异 → 真实后端模式 `serverStateToMemoryState` 全部落 default → 徽章恒灰 | 中（UI 状态展示失真；判题链路不受影响） | ✅ **已修复**（2026-09-24）：`memory-state.ts` 增 PascalCase→MemoryState 映射（NotMastered→gray / Fuzzy→yellow / Mastered→green / Proficient→gold），保留符号兼容；typecheck 0 错误 |
| 2 | **hint difficultySlot 语义错配（真实后端 hint 必失败）** | task.tsx `handleHint` 传 `difficultySlot: nextLevel`（'Partial'/'Full'），后端 `GetHintReqDto.DifficultySlot` 校验 **S1/S2/S3**（GetHintService L46，BR-30 难度档）→ 真实后端 `hint_Execute` 必返 `ParamInvalid` → 前端兜底鼓励语（功能降级为无服务端 hint）；mock getHintResDtos 亦用 'Partial'/'Full' 跟随前端而非契约 | 中（真实后端 hint 功能失效；mock 模式正常） | ✅ **已修复**（2026-09-24）：task.tsx `HINT_LEVEL_TO_DIFFICULTY_SLOT` 映射 None→S3 / Partial→S2 / Full→S1（BR-30"状态越低提示越深"对齐）；mock getHintResDtos 同步 S3/S2；"仅成功升档"逻辑保持；typecheck 0 错误 |
| 3 | **mock getSessionResultResDtos blockedPoints 缺 state** | 新契约 `BlockedPointDto = { questionId, knowledgePoint, state }`（ts-client.g.ts:1167，state 必需）；mock 第 2 条 blockedPoints 为 `{ questionId, knowledgePoint, summary }`——**缺 state、多 summary**（与真实后端 BlockedPointDto 无 summary 亦不符） | 低（mock 模式结果页卡壳项徽章恒灰/文案恒"薄弱"） | ✅ **已修复**（2026-09-24）：mock blockedPoints 补 `state: "NotMastered"`（PascalCase 对齐 #1 映射）、删 summary；连带修复 #4 |
| 4 | **result.tsx reason 判定恒"薄弱"** | L101 `bp.state === '✕' ? '卡壳' : bp.state === '△' ? '求助后对' : '薄弱'` 直接比对显示符号，而 bp.state 为后端枚举名（或 mock 缺省 undefined）→ 恒走 '薄弱' | 低（文案不精确） | ✅ **已修复**（2026-09-24）：改为经 `serverStateToMemoryState` 转换后按 MemoryState 判定（gray→卡壳 / yellow→求助后对 / 其余→薄弱），与 #1 映射联动 |
| 5 | **mock questionCount=20 与 5 题不齐** | `createStudySessionResDtos` questionCount=20，`getNextQuestionResDtos` 仅 5 题 + 1 条耗尽 → 展示"第 x/20 题"分母大于实际题数 | 低（展示性） | mock questionCount 改 5 或补足 20 条题目 |
| 6 | **mock 四态非状态驱动轮转（R6 承认）** | 四态由 scenarioOverrides 场景切换可见（error/guidance/answerSheet/degraded），非 submitAttempt queryOne 自动轮转——已知机制约束，非缺陷 | 信息 | 保留场景切换走查；如需自动轮转后续经 MockDataSpec 轮转规则（不手改 *.g.ts） |
| 7 | **taskId 类型桥接（字符串 id 落 null）** | task.tsx `taskId: search.taskId ? Number(search.taskId) \|\| null : null`——mock 任务 id 为字符串（'task-1'），`Number('task-1')=NaN → null` → createStudySession.taskId=null | 低 | 统一任务 id 契约（后端 TaskUid 字符串 vs 数字）；或按后端 createStudySession 契约调整传参 |
| 8 | **前端未跑真实后端端到端验证** | 验证止于 mock 模式 typecheck 级；createSession→getQuestion→submitAttempt→sessionResult 全链路未连真实后端；§八 1/2/3 即此缺口下审计发现的契约对齐项 | 中（接入风险） | 下一迭代真实后端端到端联调（修复 1/2/3 后），覆盖新学/复习/耗尽/错误四路径 |
| 9 | **G-3 70/30 混合比 + G-4 EndStudySession 延期** | 明确不包含项（ADR-008 不适用边界），另立决策；EndedAt==null 校验待 G-4 落地同步补 | 信息 | 后续迭代立项（70/30 混合比 / EndStudySession） |
| 10 | **题库导入管线补写 Hint** | 存量 `Questions.Hint` 为空 → 引导分支空线索（前端已兜底通用鼓励语"再想想，回忆下要点"）；导入补写为 Bank 域既有职责 | 低 | 后续项 T12 候选（经 bank.v1.json 背景钩子补写 Hint 列） |
| 11 | **会话中途退出恢复（R7 自由会话登记）** | 会话态不进 persist → 中途退出刷新丢失当前题；任务会话靠 BR-05 续做幂等（服务端恢复），**自由会话（无任务）恢复未登记** | 低 | 自由会话中途退出恢复另登记后续项 |

## 九、审核结论

- **是否可进入下一阶段：是** —— 3 处契约对齐缺陷已修复（§八 1/2/3/4 状态=已修复，typecheck 0 错误复核）；可 push（不打 tag，tag 纪律另循）。下一迭代优先：真实后端端到端联调（§八 8）。
- **遗留问题是否阻断：否** —— 无阻断项；§八 5-11 为低/信息级登记延期项，均不阻断 push。
- **下一步建议**：
  1. **真实后端端到端联调**（优先）：修复 §八 1/2/3 后，起 WebApi 连 WebH5 跑通 createStudySession → getSessionQuestion → submitAttempt 四态 → sessionResult 全链路（新学/复习/耗尽/错误四路径）。
  2. **G-3 70/30 混合比**立项（另立 ADR 决策，对接学习-BR-04 后半段）。
  3. **G-4 EndStudySession** 立项（EndedAt 写入 + 中途退出保存完整闭环，含 GetSessionQuestionService EndedAt 校验补全）。
  4. **R3/O 系列题型组件**新形态迭代（task.tsx 现按 R1/R2 渲染）。
  5. **Mic 语音输入**接线（现为占位按钮）。

## 变更记录（本文件）

| 日期 | 版本 | 变更内容 | 关联ADR |
|------|------|---------|---------|
| 2026-09-24 | V0.4.0 | 初始版本：学生作答流端到端接线审核报告（总评附条件通过；后端 360/0/2 + 生成链路全通 + 前端 typecheck 0 错误；待改进项含 3 处真实后端契约对齐问题与真实后端联调缺口） | ADR-008 |
