---
title: 小书童 - 变更记录
version: V0.3.0
last_updated: 2026-09-23
status: 活跃
---

# 变更记录

> 记录版本内的重要变更（编译修复、环境变量、框架协同等），增量维护，不覆盖历史条目。
> 格式遵循 `docs/目录结构与版本管理规则.md` §3.5 / §四。

| 日期 | 版本 | 变更内容 | 关联ADR |
|------|------|---------|---------|
| 2026-09-27 | 框架协同-v4.10.35 正式版部署 | **框架正式版 v4.10.35（4.10.35-preview.0.2）部署 + ADR80 会话缺陷正式版闭环**：框架组推送 v4.10.35 tag 并部署至 `TKWFDeployPath → F:\TKWF_FRAMEWORK_PATH`（build/refs 全部 DLL 统一 4.10.35，2026-09-26 23:03 落盘）；V0.6.6 阶段 ADR80「GraphQL 通道会话激活 Session not found」实验 3 修复随正式版并入（`WebSessionManager.NewSessionAsync` 泛型门面显式 T=SessionInfo 统一 HybridCache 类型标签）。验证：全解决方案 `dotnet build XiaoShuTong.slnx` **0 错误**（5 警告为 AdminWasm 既有 WASM/sqlite 噪音）+ 全套件 **401 通过 / 0 失败 / 2 跳过**（基线不变，框架升级无回归）；NuGet 发布已触发（nuget.org 处理中，稍后可见即可恢复消费端 NuGet 模式验证）。XiaoShuTong 基于正式版可继续推进。 | ADR80（框架会话缺陷） |
| 2026-09-26 | 领域-V0.6.7 全页面真实链路走查补齐 | **走查从 V0.6.6 的首页/排名榜推广到全部 14 个业务页面（V0.6.6 审核 §七 遗留）**。explore 全矩阵 + Oracle 评审发现**结构性根因**：`home.tsx:39-41` 用本地 useState **遮蔽** store 同名 setter（`setTasks/setReviewItems/setLearningStats`）→ 首页真实数据困在页内，review/wrong/heatmap/self/profile 5 页读空 store（"纯 mock"实质是**从未接数据源**）；14 页中 **7 页零真实调用**。修复三批次：**批次一（断链）**——P0-1 home 数据改回写 store（streakDays 保持本地，Oracle M1 决策）+ P0-2 删 `initMockData` 140 行死代码（0 引用）+ P1-1 复习项记忆状态统一 `serverStateToMemoryState`（原 `✕△○` 符号比对全落 gold）+ P1-2 复习项标题占位（`MemoryStatesDto` 无 title）；**批次二（契约错配 8 项）**——P0-3 task.tsx taskId 防退化（`Number(UId)`=NaN 恒 null，注释待后端补数值字段）/ P0-4 teacher/detail 路由 `validateSearch` 接 taskUid / P0-5 rank subject 枚举对齐 `All/语文/…/政治`（原小写英文静默失效）/ P1-3 groupUid 用 `group.groupUid`（原 `String(groupId)` 错用）/ P1-4 teacher 看板 5 处字段（students→taskList、expired→overdueCount、completedCount→completionRate、deadline→deadlineAt、key→taskUid、薄弱点 3→length）/ P1-5 parent 消费已返回字段（streakDays/weekProgress/todayCompleted/subscription）/ P1-6 `/parent/progress` 死路由改提示 / P1-7 rank `Number(id)` NaN 兜底；**批次三（零调用页接线）**——self.tsx `listBanks_Execute` 聚合学科卡（subject 中英映射 + isHot 前三）/ profile.tsx `memoryStates_Execute` 按 state 计数（不经空 store）/ review·heatmap 由 P0-1 自动复活。验证：`pnpm typecheck` **0 错误**；开发方案经 Oracle 评审（附条件通过，M1+C1-C4 闭环）；**浏览器端到端走查待 WebApi 在线补证据**（服务由用户运行）；登记后端配合项：MyTaskItemDto 缺数值 taskId / ChildItemDto.studentUid(string) vs studentId(number) / WrongQuestionsDto 缺答案字段（错题本 UI 改设计待决策）。 | 无 |
| 2026-09-26 | 领域-V0.6.6 浏览器走查联调闭环 | **三代遗留 T7「浏览器端到端走查」首次全链路打通 + 双通道会话缺陷联合闭环**：① **前端 GraphQL 契约接线修复**——根因：`main.tsx` 的 `Tkwf.configure` 漏注册 codegen 产物 `operationSelection`（Use<T>() 无子字段选择 → 400 "must have a selection"）+ **框架 tkwf-tsclient SDK 1.0.9 Use<T>() 通道不传递变量类型**（复杂 DTO 入参推断为 `$request: JSON` → HotChocolate 400 "variable not compatible"，登录因 SDK 内置 mutation 硬编码 variableTypes 不受影响故一直未暴露）；修复：WebH5 `pnpm install` 升级 **@tkwf/tsclient 1.0.10**（框架组 V1.0.10 修复 variableTypesMap 透传）+ 自研 codegen 补产 `operationVariableTypes`（`scripts/gen-ts-client.ts` FieldArg 增 gqlTypeRaw 保留 `[T!]` 可空组合，ts-client.g.ts 67 操作变量类型映射与后端 schema 逐一对照精确）+ `main.tsx` 接线 `selectionMap`+`variableTypesMap`；② **首页 DateTime 契约修复**——`home.tsx` reviewQueue.date `toISOString().slice(0,10)`（纯日期串）改完整 ISO、heatmap start/end 空串改近 30 天 ISO 区间（HotChocolate DateTime 标量不接收纯日期/空串 → 400 coerce）；③ **框架会话缺陷闭环（ADR80）**——GraphQL 通道 `loginByContext` 会话激活 `SessionException: Session not found`（REST 同输入稳定成功；会话已写入 store 但 GraphQL 通道读不到，HybridCache 局部/分布式层类型标签不一致疑似根因），框架组部署实验 3 修复（`WebSessionManager.NewSessionAsync` 泛型门面显式 T=SessionInfo 统一类型标签）+ ADR80 触发条件复验；验证：`pnpm typecheck` 0、浏览器端到端复测 **5/5 PASS**（登录/首页四查询/战力榜 Combat 三用户真实数据/战绩榜 Performance 双指标/myRanking + isMe=True 身份注入）。 | ADR80（框架会话缺陷） |
| 2026-09-25 | 领域-V0.6.5 战力榜接线 | **战力榜单指标透出 + rank.tsx power tab 真实接线（V0.6.4 §八 遗留核心项闭环）**：① **后端**——RankingItemDto/GetMyRankingResDto 新增 Streak/Volume/PkWins 单指标（RankSnapshots 对应 MetricType 冻结值直读，非求和；Value 保持 streak+volume+pk_wins 求和口径 BR-11 不变）；② **前端**——rank.tsx 删 MOCK_POWER_RANKING 硬编码改接 rankings_Execute/myRanking_Execute（metric='Combat'），统一数据流 fetchBoard 按 tab 切换 Combat/Performance，战力榜展示"连续 X 天 · 背诵 Y 篇 · PK胜 Z 次"（pkWins>0 才显示），RankEnabled 门控仅约束战绩榜（BR-02/04）；③ **数据就绪**——联调种子 5.4 直插战力快照（3 用户 × Streak/Volume/PkWins，PkWins 切片 07 未实施 → 0），mock data.ts 补新字段（rankings ×3 + myRanking ×2）；④ **契约**——buildSchema.ps1 + gen-mock 全链路刷新（ts-client.g.ts selection 含 streak/volume/pkWins，仅增字段无破坏性变更）。全套件 **401 通过 / 0 失败 / 2 跳过**（基线 398 +3），`pnpm typecheck` **0 错误**。 | 无 |
| 2026-09-25 | 领域-V0.6.4 结果页战绩榜掌握度接线 | **结果页正确率透出 + 战绩榜 accuracy/mastery 双指标接线（V0.6.3 审核报告 §七-2 缺口①/② 落地）**：① **后端**——GetSessionResultResDto 新增 Accuracy（0~1，CorrectCount/TotalCount 派生，SessionResultAggregator 零改动，空会话=0）；RankingItemDto/GetMyRankingResDto 新增 Accuracy/Mastery 单指标（RankSnapshots 对应 MetricType 冻结值直读，非求和；Value 求和口径 BR-11/12 不变）；② **前端**——result.tsx 展示"本次正确率 X%"（复用 accuracyToPercent，V0.6.2 L122 裁决：原始 01 前端映射百分比）；rank.tsx 删硬编码 mock（MOCK_SCORE_RANKING）改接 rankings_Execute/myRanking_Execute（scopeId=联调种子群 UId `group-demo-73` 占位，群组上下文接入后替换），战绩榜双指标"正确率 X% · 掌握 Y%" + RankEnabled=false 隐藏/禁用战绩 tab（BR-02 门控，UI 文档 L249/L553），战力榜保留 mock 标注（接线另立）；③ **数据就绪**——mock data.ts 补 getRankingsResDtos/getMyRankingResDtos 含 accuracy/mastery + rankEnabled（initialData + motivational 场景）；联调种子群 UId 落常量 `SeedGroupUid="group-demo-73"` + 补 2 学生成员 + DailyStats×3 + KnowledgeMastery×3 + 直插 RankSnapshots×6（3 用户 Accuracy+Mastery 双指标，FreezeJob 可跑）；④ **契约**——buildSchema.ps1 + gen-mock 全链路刷新（schema.graphql / ts-client.g.ts / ts-client.mock.g.ts 均含新字段，仅增字段无破坏性变更）。全套件 **398 通过 / 0 失败 / 2 跳过**（基线 394 +4：Accuracy 0.75 / 战绩榜双指标 / 缺失默认 0 / MyRanking 单指标），`pnpm typecheck` **0 错误**；WebApi build 中途 MSB3492（AssemblyInfoInputs.cache 读取失败）为环境性瞬时告警，复跑即消，契约产物 grep 核验含新字段不影响正确性。 | 无 |
| 2026-09-25 | 领域-V0.6.3 真实端到端联调 | **首次真实启动 WebApi（历 V0.4.0~V0.6.2 五连优先建议）**：① **SQLite 文件库切换**——DomainOptions.FreeSqlDataTypeName 配置键 + Program.cs 显式 `UseFreeSqlEntityDAC(DataType,...)` 重载（XML 明示无参重载不设 DataType 默认 PG），appsettings.Development 切 `Data Source=XiaoShuTong.dev.db`（生产 appsettings.json 保持 PG 不动），Domain.csproj 补 `FreeSql.Provider.Sqlite`+`Microsoft.Data.Sqlite`（Dll 模式 refs 不传 NuGet 依赖），`.gitignore` 追加 `*.dev.db`；② **登录放行（联调专用）**——`OnLoginByWeChatAppletAsync` 白名单映射（xiaoming=10001/owner01=10002/parent01=10003）+ `DomainHost.Options.IsDevelopment` 运行时守卫（Oracle 闭环#1），前端 `authType:'wechat'`→`'WE_CHAT_APPLET'`+`'MOBILE_WEB'`（非法枚举修正）；③ **最小链路种子**——OnEnsureDataReadyAsync 从空壳补 Groups/GroupMembers/Banks/Questions/Tasks/TaskAssignments/ParentStudentRelations（UId 显式赋值 + CreateScope 包裹 Scoped IFreeSql，Oracle 闭环#2）；④ **端口/代理统一**——vite proxy 5157→5020、mock-server 注释 5156 勘误、schema-settings 5000→5020（五处冲突收敛）；⑤ **historyAccuracy 前端消费**——家长端 dashboard 删硬编码 MOCK_SUBJECT_MASTERY 改接 `dashboardReport_Execute` 的 `subjectsMastery.accuracy`（0~1 → 百分比 + 四阶阈值 ✕△○★，Oracle 闭环#4），mock data.ts submitAttemptResDtos **5 数组 8 成员**补 historyAccuracy（Oracle 闭环#3）。全套件 **394 通过 / 0 失败 / 2 跳过**（基线不变），`pnpm typecheck` **0 错误**；WebApi 实证启动成功（SQLite 自动建表/视图 + 种子 + schema export + 监听 5020）；浏览器端到端走查因用户指示跳过（联调阻塞已消解，登录守卫修复后留待用户环境验证）。 | 无 |
| 2026-09-25 | 领域-V0.6.2 掌握度透出与走查缺口复核 | **走查缺口复核（附录 4 项：①②③ 已闭环实证 + ④ 唯一真实缺口）+ masteryLevel 契约透出**：走查文档《背记场景模拟走查-历史》附录 4 缺口经 git 核验——① JudgingEngineService 接入（d3c1a56）/ ② needsGuidance / ③ attemptCount+maxAttempts（93f9f78 + 39ebf50）**已闭环**；仅 ④ masteryLevel 真实。`SubmitAttemptResDto` 新增 `HistoryAccuracy`（double 0~1，近 20 次正确率 Correct=1/Partial=0.5/Wrong=0 均值，PRD L187 间隔系数数据基础），**四处返回路径透出**：正常路径 `state==null?0.8:newAccuracy`（新题首答中性，与间隔计算口径一致）/ 幂等命中 BuildFromExistingAsync `state?.HistoryAccuracy??0.8`（实施中发现第 4 路径）/ Full 早退 / Play；契约四文件 buildSchema.ps1 + gen-mock 全链路刷新（schema.graphql / GraphQL_Api.md / ts-client.g.ts / ts-client.mock.g.ts 均含 historyAccuracy）；**清理**：删 LocalJudgmentEngine.cs 死代码（0 调用者）+ SubmitAttemptService L23 注释整行审查（切片桩字样全部过时）；**文档**：走查附录标注复核结论（防误开工）+ Business.md 学习-BR-25 透出口径增订（不覆盖语义）+ LOG.md 记录；新增 5 用例（加权均值 0.5/新题中性 0.8/Full 存量/Play 存量/收敛 1.0）。全套件 **394 通过 / 0 失败 / 2 跳过**（基线 389 +5）。 | 无（ADR-003/008 关联参考） |
| 2026-09-25 | 领域-V0.6.1 任务进度重算热路径优化 | **BR-24 Progress 重算热路径优化（ADR-010 实现路径增补，Oracle 评审 7 条闭环）**：`SyncTaskProgressAsync` 从"每次作答全量跨会话重算（O(N²)）"改为 **"增量维护 + 近完成全量校验"**——TaskAssignments 新增内部字段 `ConsumedQuestionIds`（string 逗号分隔，`[DtoFieldIgnore][JsonIgnore]` 不进 Dto/对外契约，schema.graphql 零变化已实测）；每次作答仅按 QuestionId 查本题跨会话 attempts（O(1-2)）判定"新消费"入集合，Progress = 集合大小 / Tasks.QuestionCount（判定口径/状态迁移/Completed 幂等/个人会话豁免全不变）；近完成（set.Count ≥ total-1，Oracle 闭环#4 防漂移漏触发）触发一次全量重算校验重建集合（防并发/幽灵/升级漂移）；特有坑修复——QuestionId 全域 string 业务键（HashSet<string> + StringComparer.Ordinal）、损坏集合串 ParseConsumedSet 安全降级；Business.md BR-24 实现口径追加 + LOG.md 记录；新增 6 用例（跨会话累积/同题幂等/达上限不重复/幽灵重建/损坏串降级/空集合升级收敛）。全套件 **389 通过 / 0 失败 / 2 跳过**（基线 383 +6）。 | ADR-010 |
| 2026-09-25 | docs-场景走查 | **落盘《背记场景模拟走查（历史）》**（`docs/1-需求分析/V1/背记场景模拟走查-历史.md`）：补录 2026-09-22/23 会话中"模拟用户用小书童背记历史"的产品走查内容——① 7 场景全景（任务执行/自由背诵/复习队列/错题本/PK 对战/家长报告/群主管理，各含 UI 框图 + 📌产品机制标注）；② 记忆状态流转总览（单知识点跨 7 天 ✕→△→○→★ 生命周期表 + 设计精髓）；③ 唐宋背记完整交互流程（6 屏逐屏 UI 文案 + GraphQL 调用 + SubmitAttemptResDto/GetHintResDto 契约 + 前端分支决策代码，含"答对/部分正确/答错引导/引导后仍错兜底"全分支）；④ JEV 模型适配性分析（JEV→架构映射全 ✅ + 3 契约缺口 needsGuidance/attemptCount/masteryLevel）；⑤ 产品机制映射总表（7 场景 → 机制）；⑥ 附模拟暴露的真实缺口（JudgingEngineService 未接入 SubmitAttempt 等 4 项）。 | 无 |
| 2026-09-24 | 领域-V0.6.0 任务进度联动与Hint补写 | **BR-24 TaskAssignments.Progress 联动（学习-BR-24 落地）**：SubmitAttemptService 新增 SyncTaskProgressAsync——重算式（跨会话聚合经 SessionId→StudySessions.TaskId 桥接，Correct 或按题计数 ≥MaxAttempts 判消费，Attempts 无 AttemptCount 字段 Oracle 勘误）、Pending→InProgress（首次推进）、Progress≥100→Completed+CompletedAt、三预留字段（SessionId/StartedAt/CompletedAt）接线、个人会话/Full/Play/幂等豁免、Completed 幂等（AllowRedo 重做不改变）；Business.md 增量修订（BR-24 口径/§3.3 状态机 Overdue→Completed 允许+C-18 例外+C-20 补列）；**Hint 补写（ADR-010 决策六）**：ImportBankJsonService JSON 四件套批量导入（knowledge_card_id join 背景钩子卡片 fields.记忆钩子、Content card 镜像修复、幂等键 QuestionId upsert、JsonPath 生产首选）+ BankHintBackfillJob 存量回填（KnowledgePoints[0] 精确/包含匹配、未命中留空计数）+ 前端 O4 连线组件（点击配对、提交 JSON {"pairs":{...}}）+ O3 swipe（framer-motion 左滑对/右滑错，双键降级保留）+ mock O4 pairs 样例。全套件 **383 通过 / 0 失败 / 2 跳过**（基线 367 +16：BR-24 7 用例 + JSON 导入 6 + 回填 3），前端 typecheck **0 错误**（buildSchema/gen-mock 契约同步 importBankJson_Execute 全链路）。 | ADR-010 |
| 2026-09-24 | 领域-V0.5.0 会话收尾与题型扩展 | **G-3 70/30 混合比出题（学习-BR-04 后半段落地）**：GetNextQuestionService 分池（复习池=到期非熟练 / 新题池=无状态）+ **按已答复习占比动态池选择**（ReviewRatio=0.7 常量，无状态天然交错趋近 7:3，Oracle 评审闭环替代窗口交错）；**G-4 EndStudySession**：新建 EndStudySessionService（BR-39 归属 + 幂等回读 + 聚合写回 EndedAt/CorrectCount/TotalTimeMs）+ SessionResultAggregator（Learning 域 internal 共享口径）+ GetSessionQuestionService EndedAt 校验（SESSION_ENDED 3003，ADR"3002"笔误勘误）+ BR-05 幂等随 EndedAt 生效；**G2 防爬修复**：StripAnswerFromContent 按题型白名单剥 answer/keywords/correct_option（BR-19 真正落地，R4 answer=卡片正文特例保留）；**前端 R3/O 题型 + Mic 语音**：QuestionCard 按题型分发（R3a/R3b textarea、O1 单选、O2 多选、O3 对错双键、R4 展示）+ parseQuestionContent 双兼容 + use-voice-input（系统输入法主路径 + Web Speech API 兜底，规范 §6.1 合规）+ endStudy 前端接线（题集耗尽/中途退出 fire-and-forget）+ mock 补 R3a/O1/O2/O3 + endStudy 表 + content JSON 形态。全套件 **367 通过 / 0 失败 / 2 跳过**（基线 360 +7：混合比 1 + EndedAt 负例 1 + EndStudy 5），前端 typecheck **0 错误**；遗留项（真实后端端到端联调、O4 连线、O3 swipe、BR-24 Progress 联动 V0.6.0+）不阻断。 | ADR-009 |
| 2026-09-24 | 领域-V0.4.0 学生作答流端到端接线 | **G-1 getSessionQuestion 契约暴露（Learning 薄包装+BR-06 归属校验，Bank Callee 保持 internal，响应复用 GetNextQuestionResDto）+ G-2 判题元数据去桩（Keywords 原样传递 Weight/Required/Aliases 保留 + Questions.Hint 列 + 批量预取防 N+1，注册表降级测试专用）+ 前端 task/result 四态接线（createStudySession→sessionUid 活源→判题四态→题集耗尽→sessionResult）+ mock 补数（submitAttempt 4 条新契约/getNextQuestion 6 条/语文 hint/六场景覆盖）**：后端全套件 **360 通过 / 0 失败 / 2 跳过**（基线 353 净增 +7：GetSessionQuestionServiceTests 6 用例 + Hint 去桩回归），前端 `pnpm typecheck` **0 错误**；遗留项（真实后端端到端联调、memory-state/hint difficultySlot/mock blockedPoints 契约对齐、70/30 与 EndStudySession 延期）不阻断。 | ADR-008 |
| 2026-09-23 | 领域-判题契约扩展（步骤6） | **审核报告完成（步骤6 收官）**：产出 docs/草稿/判题契约扩展-needsGuidance-attemptCount-审核报告.md（总评附条件通过；证据：全套件 353/0/2 复跑 + WebH5 typecheck 0 错误；遗留项 task.tsx 接线/sessionUid 源/钩子单测，不阻断）；方案 §十 步骤 0-6 全部勾选。 | 无 |
| 2026-09-23 | WebH5-判题契约扩展（步骤5） | **WebH5 useAttemptFlow hook 新建**：`src/XiaoShuTong.WebH5/src/hooks/use-attempt-flow.ts` 包一层 `Tkwf.User.Use<SubmitAttempt_ExecuteService>().submitAttempt_Execute`，按方案 §4.3 三分支决策（**showAnswer 优先兜底** → needsGuidance 引导 → celebrate 庆祝）消费 5 新字段（needsGuidance/attemptCount/maxAttempts/showAnswer/isDegraded），返回判别联合动作描述符 + 原始 `SubmitAttemptResDto` 透出（postState 记忆徽章 / isDegraded 降级标记）；域级失败（success=false）不抛错产 `{type:'error',code:errorCode}`，RPC 抛错捕获 `DomainClientError.code` 进 error 状态；`typecheck`（tsc --noEmit）0 错误；sessionUid 由调用方传入（createStudySession 接线为后续项）。 | 无 |
| 2026-09-23 | 领域-判题契约扩展（步骤3） | **判题契约扩展步骤3：T1/T3/T4 用例补全 + needsGuidance/showAnswer/hint 决策修正**：① Service 决策修正——`showAnswer` 在 `(Wrong \|\| Partial) && attemptCount >= MaxAttempts` 时置 true（决策表 #4 partial@2 / #8 wrong@2 均兜底，防引导死循环；旧逻辑仅 Wrong 达上限，partial@2 残留 needsGuidance=true 造成无限重试）；`Hint` 改为 `(needsGuidance \|\| hintLevel==Partial)` 时非空（决策表 #2/#5：引导分支必带线索，hintLevel=None 亦给 S1 首字/意象线索，BR-29 ≤20 字）；② 新增 Contract 用例 **T1**（correct 无引导→needsGuidance=false/showAnswer=false，postState 升级 BR-11 △→○）/ **T3**（partial 达上限→needsGuidance=false/showAnswer=true，决策表 #4）/ **T4**（wrong 第 1 次→needsGuidance=true、hint 非空 ≤20 字，BR-29）；③ 方案 §六 伪码同步（showAnswer 含 isPartial + Hint 决策注释）并勾选 §十 步骤 0-4；④ 全套件 **353 通过 / 0 失败 / 2 跳过**（基线 350 + 3）。 | 无 |
| 2026-09-23 | 领域-判题契约扩展 | **SubmitAttemptResDto 判题契约扩展（needsGuidance/attemptCount/maxAttempts/showAnswer/isDegraded）**：① 响应 DTO 新增 5 字段（方案 §五），前端作答流由服务端契约驱动（正确/部分/错误 × 引导/重试/兜底 → 下一题全分支）；② 服务端统计 attemptCount + 决策 needsGuidance/showAnswer（达上限 `MaxAttempts=2` 展示答案防死循环，BR-16 Full 对齐）；③ `isDegraded` 透出（LLM 降级标记）；④ **幂等键细化**——Attempts 实体新增 `AnswerHash`（SHA-256 + 唯一索引 `idx_attempts_idem`），BR-27 从"同会话同题唯一"微调为"同会话同题同答案唯一"：网络重发防抖保留、**引导后重试放行**（决策记录见方案 §6.1）；⑤ 新增 4 分支用例（T2/T5/T6/T8）+ 更新幂等测试，全套件 **350 通过 / 0 失败 / 2 跳过**。 | 无 |
| 2026-09-23 | 领域-判题引擎接入 | **SubmitAttemptService 接入 JudgingEngineService（替换 LocalJudgmentEngine 桩）**：① 判题链路升级为五键契约（BR-32~36：同义词组 BR-33 / required 强制 partial BR-32 / 加权命中率 / 阈值 0.85/0.50 BR-35）+ `PreferLlm` 交统一 AI 网关（LlmGateway round-robin + 降级标记，平台-BR-02/03/04，无启用模型安全降级 BR-04）；② 题库跨模块桩 `AnswerKeywords`（string[]）映射为 `KeywordGroup[]`（默认组 weight=1/required=false）；③ 新增 Contract 测试锁定 0.50 边界判 Partial（旧桩 0.60 阈值判 Wrong，实证接入生效）+ LLM 降级链不抛异常；④ 全套件 **346 通过 / 0 失败 / 2 跳过**（基线 345 + 1）。判题契约 DTO 扩展（needsGuidance/attemptCount/showAnswer/isDegraded）为方案步骤 1，见 `docs/草稿/判题契约扩展-needsGuidance-attemptCount-方案.md`。 | 无 |
| 2026-09-23 | 文档-规范体系 | **AGENTS 规范体系补全**（Oracle 评审后修正）：① `PROJECT_STRUCTURE.md` 重写消除失实——Dev.slnx 幽灵项、AdminWeb 旧名、根目录项目/脚本误记，改为实际 src/ 结构 + 勘误备忘表；② `Agents_Use_TKWF.md` §1 勘误（Dev.slnx 不存在）+ §3 新增「进度同步纪律」（四类文档联动表：变更记录/README 状态清单/docs AGENTS/PROJECT_STRUCTURE/子产品 AGENTS）；③ 新增 3 个子产品 AGENTS.md（`src/XiaoShuTong.AdminWasm` 管理端 / `tests/XiaoShuTong.Tests` 测试 / `题库` 内容资产），规范体系升级为三层（L0 根规则 → L1 文档路由 → L2 子产品）；④ `.TKWF/TKWF-Rules.md §四` 清理 build.ps1/build-quick.cmd 幽灵项（仅存 buildSchema.ps1）；⑤ `docs/AGENTS.md` 路由表补 PROJECT_STRUCTURE 入口、`README.md` 目录树加指向声明。 | 无 |
| 2026-09-23 | 题库-化学九下 | **化学九年级下册按新版教材复核重建（化学确认加入背诵）**：新版下册为第八~十一单元（旧版第十~十二单元"酸和碱/盐 化肥/化学与生活"重组为"常见的酸、碱、盐/化学与社会"）——① 第八单元金属和金属材料（金属之最/合金/金属活动性顺序/置换反应）；② 第九单元溶液（溶液特征/溶解度/结晶/溶质质量分数）；③ 第十单元常见的酸碱盐（pH/指示剂/酸五性碱四性/中和反应实质/盐的俗称与检验）；④ 第十一单元化学与社会（六大营养素/微量元素/合成材料/白色污染）。背诵内容/训练/背景钩子/篇目清单四件套同步重建；物理/数学保持"仅整理资源不参与背诵"。 | 无 |
| 2026-09-23 | 题库-物理化学 | **物理/化学/数学素材与策略更新**：① 素材新增化学九下（第八~十一单元：金属/溶液/酸碱盐/化学与社会）、物理**沪科技版**八全+九全（**非人教版**，人教作对照）、数学七上~九上；② **资源策略变更——物理、化学、数学仅整理资源，不参与背诵**（背诵产品核心在语文/英语/历史/道法/地理/生物）；化学九上既有四件套保留、九下待确认；③ 同步更新 `各册更新情况清单.md`/README/AGENTS.md（物理版本更正为沪科技版）。 | 无 |
| 2026-09-23 | 题库-道法九上 | **道德与法治九年级上册四件套全量重建**（对照 2024 修订版教材 PDF）：新版为**全新 4 单元 10 课结构**——第一单元坚持党的全面领导（百年大党风华正茂/坚持以人民为中心/党是领导一切的）、第二单元进入新时代（新的历史方位/新时代的思想旗帜/中国腾飞谱新篇）、第三单元奋进新征程（擘画宏伟蓝图/实现高质量发展）、第四单元共圆中国梦（凝聚团结伟力/实现伟大梦想）；旧版 4 单元 8 课（富强与创新/民主与法治/文明与家园/和谐与梦想）作废。清单/背诵内容(约174记忆点)/训练/背景钩子已重建。 | 无 |
| 2026-09-23 | 题库-清单 | **新建《各册更新情况清单》**（`题库/各册更新情况清单.md`）：索引各学科各册教材素材到位情况（PDF 矩阵）与题库复核/重建进度（语文/英语/历史/道法/地理/生物/化学 逐册状态）；**备注：九年级下册系列教材尚未发布，待 2027 年寒假或开学前检查**发布情况后补素材复核。README 进度与待办同步更新。 | 无 |
| 2026-09-23 | 题库-素材补全 | **教材素材补全 + 地理/生物/语文九上/英语词汇全量复核**（`F:\_教材素材\初中` 补到 7 新 PDF：历史九上、地理七下/八上/八下、生物八上/八下、语文九上）：① **历史九上《世界历史》四件套全量重建**——2024 新版为 7 单元 22 课全新结构（旧版 8 单元 14 课作废：美国内战/俄国改革/明治维新/第二次工业革命等移入九下），新增第1课"从原始社会到奴隶社会"、第12课"中古时期的非洲和美洲"，第13课重组为"西欧经济社会发展与文艺复兴运动"；② **地理七下/八上/八下完成**——七下按新版 7-11 章区域地理重建（删旧版"发展与合作/中国自然环境"，"中东"改"西亚"）；八上按新版 5 章重写（新增第五章"建设美丽中国"、矿产资源/海洋资源两节）；**八下四件套新建**（6-10 章中国四大地理区域，此前缺失）；③ **语文九上复核**——28 篇目与 2026 新版教材全保留（曹刿论战/邹忌讽齐王纳谏/陈涉世家/出师表/诗词曲五首整组移入本册），R1/R4 文本逐条核对一致，统一重复篇目 kp 命名；④ **english JSON 词汇库重建**——从新版教材 Vocabulary in Each Unit 提取 5 册核心词汇（310-381 词/册），生成 R1(英→中)/R2(中→英) 成对题 **3428 题**，废弃旧版 apple/library 示例。 | 无 |
| 2026-09-23 | 题库-生物扩展 | **生物模块对照复核 + 扩展**（八上/八下素材到位）：① 模块 A/B/C 清单加 2026-09-23 版本对照注记（七下"植物的生活"+第四单元人体生理(一)；八上第四单元人体生理(二)+第五单元生物与环境）；② **新增模块 E**（八下"生命的延续和发展"：生物的生殖/遗传与变异/进化/生物多样性，四件套）；③ **新增模块 F**（八上"生物与环境·生态安全"：生态系统/食物链/生物圈/生态安全，四件套）；④ 模块 D 补充 **D9 用药与急救、D10 选择健康的生活方式**（新版八上第七章第三/四节）。 | 无 |
| 2026-09-23 | 题库-地理完成 | **地理 7-9 年级题库全部完成**：七下重建（7-11 章：亚洲→邻近地区→东半球→西半球→极地，14 节约 197 记忆点）+ 八上重写（5 章 16 节约 290 记忆点，补"建设美丽中国"章）+ 八下新建（6-10 章 13 节约 208 记忆点：中国地理差异/北方/南方/西北/青藏），四件套（清单/背诵内容/训练/背景钩子）全部对齐 2024 修订版教材。 | 无 |
| 2026-09-23 | 题库-历史九上 | **历史九年级上册《世界历史》四件套全量重建**（2024 修订版，7 单元 22 课约 345 记忆点）：第一单元文明的产生和古代亚非文明（第1课从原始社会到奴隶社会为新版新增）→ 第二单元古代欧洲文明 → 第三单元封建时代的欧洲 → 第四单元中古时期的亚洲非洲美洲（第12课中古时期的非洲和美洲为新版新增）→ 第五单元走向近代（第13课西欧经济社会发展与文艺复兴运动为重组课）→ 第六单元资本主义制度的初步确立 → 第七单元第一次工业革命和马克思主义的诞生（第22课活动课唱响《国际歌》）；原旧版 8 单元 14 课框架（含"亚历山大帝国""基督教的兴起"等旧课）作废。 | 无 |
| 2026-09-22 | 题库-地理七上复核 | **地理七年级上册按 2024 修订新版重写四件套**：新版为 6 章结构（地球/地图/陆地和海洋/天气与气候/居民与文化/发展与合作），旧版为 5 章（第一章 地球和地图 合并）；新增考点「地球的宇宙环境」（太阳系八行星、人类太空探索）与「第六章 发展与合作」（发达国家/南北对话）均在重写中补齐；覆盖 清单/背诵内容(6章约50题)/训练/钩子。七下/八上（中国地理）素材缺失待补。 | 无 |
| 2026-09-22 | 题库-生物版本注记 | **生物模块 A 清单版本对照注记**：对照人教版生物七上 2024 新版，确认原"第一单元 生物和生物圈"为旧版单元名，新版七上第一单元改为"生物和细胞"（认识生物/认识细胞/从细胞到生物体）；考点（生物特征/细胞结构/结构层次）仍有效故不重写，模块 A-D 跨册、素材仅七上/七下，完整复核待补八年级素材。 | 无 |
| 2026-09-22 | 题库-英语重建 | **英语初中三册（七上/七下/八上/八下/九上）按 2024 修订新版主题式教材全量重建**：① 发现原英语题库为旧版 Go For It! 结构（七下 Unit1 Can you play the guitar? 等），2024 新版为全新主题式单元；② 新式英语背诵库形态定为「主题+核心句型+语法焦点」，废弃旧版"句型逐句翻译"；③ 已完成：七上（Starter1-3+Unit1-7）、七下（8单元）、八上（8单元）、八下（8单元）、九上（8单元）共 5 册背诵内容/训练/背景钩子 + 篇目清单；④ 删除旧 Go For It 九上残留文件。注：english JSON 词汇库仍为旧示例，待同步。 | 无 |
| 2026-09-22 | 题库-内容复核 | **英语七年级（新版主题式）重建**：① 重大发现——原英语题库为旧版 Go For It! 结构（七下 Unit1 Can you play the guitar? 等），2024 新版为全新主题式单元；② 按新版重建七下（8 主题单元 Animal Friends/No Rules/Keep Fit/Eat Well/Here and Now/Rain or Shine/Once upon a Time/A Day to Remember）+ 七上（Starter 1-3 + Unit 1-7，10 模块），定位为「主题式核心句型 + 语法焦点」新形态；③ 已产出七下四件套（清单/背诵内容/训练/背景钩子）+ 七上（清单/背诵内容）。注：会话上下文受限，八上/八下/九上及生物、地理复核待续。 | 无 |
| 2026-09-22 | 题库-内容复核 | **语文七/八年级原文复核完成**（对照 2024 修订版统编语文教材 PDF）：① 篇目清单与教材目录核对一致（古诗文篇目稳定，无结构变化，区别于历史/道法的框架重建）；② 逐篇核对整篇默写（R3b）原文，**修正 5 处教材/题库标点与用字差异**——《观沧海》"若出其中**；**星汉灿烂"（分号）、《次北固山下》"乡书何处达**，**归雁洛阳边"（逗号非问号）、《登幽州台歌》"独怆然而涕下**！**"（叹号）、《过松源晨炊漆公店》"**政**入万山**围子**里，一山**放出**一山拦"（教材注释 d/e/f 明确"政=正、围子=圈子、放出=放过"）、《木兰诗》"**唯**闻女叹息"；③ 修正同步至 背诵内容/背诵训练/背景钩子 全链路（JSON 6 处 + txt/md 同步）；④ 确认其余 15 篇整篇默写与教材一致（闻王昌龄/天净沙/峨眉山月歌/江南逢李龟年/夜雨寄北/竹里馆/春夜洛城闻笛/逢入京使/晚春/泊秦淮/贾生/约客/登飞来峰/赤壁/如梦令）。 | 无 |
| 2026-09-22 | 题库-内容复核 | **历史/道法 8 册四件套原文复核完成**（对照 2024 修订版统编教材 PDF 全文）：① 重大发现——原历史/道法题库为**旧版教材结构**（历史七上自标"4单元18课"实为2016旧版16课框架；道法各册为2017/2018旧版框架），2024 修订版重构（历史七上 16→21 课、八下 21→24 课；道法七上 10→13 课、七下 10→11 课、八上 10→12 课、八下 8→14 课），新增良渚/陶寺古城、夏商周/秦汉科技文化、民族交融、宪法法治体系等新知识点；② 按教材全文重写 32 个文件（8 册 × 四件套：清单/背诵内容/背诵训练/背景钩子），历史 4 册 100~111 题/册、道法 4 册 51~66 题/册，S1-S4 分阶全配齐；③ 道法背景钩子统一命名为 `-背景钩子.md`（删除旧版 `-知识卡片.md` 4 个遗留文件）；④ 教材素材：语文七/八年级 4 册 PDF 已到位待复核；地理人教版仅七上可复核（八下缺口待补素材）；历史/道法/英语九年级 PDF 待补。 | 无 |
| 2026-09-13 | AdminWasm-V0.1.8 | **G7b 验证清零（框架 v4.10.21 部署后）**：框架组 v4.10.19 修复 `DateTime?` nullable TypeHandler（`TypeHandlers[typeof(DateTime?)] = DateTimeNullableUtcHandler` 精确匹配 → MapType(string) 建 TEXT 列，Oracle C2 落档 ADR69），v4.10.21 合并交付部署——XiaoShuTong **全套件 345 通过 / 0 失败 / 2 跳过（V0.1.8 全绿达成）**；`verify-g7b.ps1` 验证脚本特征名对齐框架实现（DateTimeNullableUtcHandler）；审核报告 G7b 遗留缺陷闭环。 | 无 |
| 2026-09-13 | AdminWasm-V0.1.8 | **Tier 1.5 回归修复（框架 v4.10.18 部署后）**：① G8——`GetNextQuestionService` 知识点过滤移内存层（FreeSql SQLite provider 无法翻译 string[] `.Contains()`，PG jsonb `@>` 无对应，框架文档明示边界；单题库 <1000 题量级合理）；② `GetDashboardReportService` `weekStart` 改周一起点公式 `today-((DayOfWeek+6)%7)`——原 `today.AddDays(-(int)DayOfWeek)` 周日（DayOfWeek=0）退化漏周六（09-13 实证 WeekProgress=5 而非 8）；③ 全套件 338 通过 / 7 失败 / 2 跳过（净增 4 清零：G8×3 + GetDashboardReport×1）；④ **剩余 7 失败 = 新框架缺口 G7b**——DateTimeUtcHandler 只注册 `typeof(DateTime)` 未覆盖 `DateTime?`（nullable），FreeSql 按 `DateTime?` 查 TypeHandlers 不命中 → 走原生路径 +8h；官方 G7 测试实体是非空 DateTime，nullable 场景未覆盖——已记录框架问题单（`5bc70d2`），待框架组补 `TypeHandlers[typeof(DateTime?)]` 注册。 | 无 |
| 2026-09-12 | AdminWasm-V0.1.8 | **Tier 1.5（SQLite :memory: 真实视图）恢复**（框架 v4.10.14 G6b 修复后）：① Fixture 切官方 `UseFreeSqlEntityDAC(Sqlite, "Data Source=:memory:")` + 集合 `DisableParallelization=true`（ADR66 串行契约）；② 新建 `XiaoShuTongTestBase` 中间基类（每 Fact 前 `OnInitializeAsync` → `Host.ResetTier15SqliteMemoryDbAsync` 原地清空），66 个测试类继承迁移；③ `GetPkStatsServiceTests` 恢复 5 Fact 真实视图聚合（仅 Finished 计入 / RLS / 胜率 API 层 / 银行家舍入 0.62）；④ 修复 21 个测试种子缺 UId（实体未实现 IEntityTracked，框架不自动生成；MockDb 无唯一约束掩盖，SQLite 暴露）——Tier 1.5 下 335 通过 / 10 失败；⑤ **剩余 10 失败为框架缺口**（G7：DateTime UTC Kind 往返丢失 +8h，7 用例；G8：string[] Contains 查询 SQLite 方言翻译失败，3 用例）——已记录框架问题单，待框架组修复（不绕过）。 | 无 |
| 2026-09-12 | AdminWasm-V0.1.7 | **框架 v4.10.8 适配 + Tier 1.5 迁移尝试**：① 测试项目加 `FreeSql.Provider.Sqlite` 依赖 + `TKWF.Domain.Testing.Mock` 引用（框架 targets 未自动注入 Mock——LlmGatewayTests 需 `MockDbEntityDAC.Clear()` 隔离）；② `PkPlayerStatsView` 补 `ViewSqlSQLite` 方言变体（FILTER→CASE WHEN，SQLite 测试用）；③ **Tier 1.5（SQLite :memory:）尝试后发现框架缺口**——`ConfigTestDomainAsync` + `UseFreeSqlEntityDAC(Sqlite,:memory:)` 的 `SyncViewsAsync` 抛 `ObjectDisposedException`（FreeSql SQLite 连接池 `:memory:` 不保活，SyncStructure 后连接释放），**框架自身测试全用裸 FreeSqlBuilder（绕过 DomainHost），此组合未实证**——回退 Tier 1（MockDbEntityDAC）基线，聚合验证标注待框架修复；④ `.gitignore` 补 `src/**/.xCodeGen/Generated/`（生成物误跟踪修正）。 | 无 |
| 2026-09-10 | AdminWasm-V0.1.0 | XiaoShuTong.AdminWasm（原 XiaoShuTong.Wasm）迁移 AntDesign Blazor Pro 脚手架（AntDesign.ProLayout 1.6.2 + Pro 布局/登录/菜单骨架，保留 TKWF ApiClient/GraphQL/WasmAuth 集成），项目与目录改名 XiaoShuTong.AdminWasm；建立 AdminWasm 独立 MinVer 版本线（csproj `MinVerTagPrefix=AdminWasm-V`，与领域线 V0.x tag 隔离）。 | 无 |
| 2026-09-09 | V0.3.1 | `GetWrongQuestionsService` 跨域同名合并：Stats 版（StatsWrongQuestions 控制器）删除，并入 Learning 版（Summary 富化批处理 + DTO 对齐 `{items,total}` 分页决策）；GraphQL 契约统一 `wrongQuestions_Execute`；测试迁移至 Learning 目录。同期修复 HotChocolate 版本漂移（CPM 16.5.1 → 16.6.4 对齐框架部署 refs 4.9.114，schema export 方可用）。 | 无 |
| 2026-09-09 | V0.3.0 | `Directory.Build.props` 新增 `_TKWF_ValidateDeployPath` 目标（`BeforeTargets="ResolveAssemblyReferences"`）：Dll 模式检测到环境变量 `TKWFDeployPath` 为空时，提前报清晰指引错误，替代原先 `build\refs\...` 相对路径导致的 CS0006/MSB3245 误判。NuGet 模式（CI）不触发。 | 无 |
