---
title: 任务进度重算热路径优化 - 审核报告
version: V0.6.1
summary: SubmitAttemptService 任务进度重算 O(N²)→增量集合+近完成校验（BR-24 实现优化，口径不变）
date: 2026-09-25
status: 通过
dev_version: V0.6.1
auditor: Sisyphus（证据审计）
---

# 任务进度重算热路径优化 - 审核报告（V0.6.1）

## 一、审核范围

- 开发版本：V0.6.1（任务进度重算热路径优化）
- 开发方案：`docs/V0.6.1-任务进度重算热路径优化-开发方案.md`（Oracle 评审 7 条闭环后版本，T1-T5）
- 架构决策：`docs/adr/ADR-010-任务进度联动契约.md`（活跃；"实现路径增补（2026-09-25）"登记增量集合 + 近完成校验，非重新决策）
- 审核日期：2026-09-25
- 审核方式：主 Agent 直接实施（用户指示"继续，不要委派"，延续 V0.6.0 实施模式）+ 证据核验——后端 `dotnet test` 389/0/2、契约零变化实测（schema.graphql / Dto grep）
- 审计对象文件清单：

| 层 | 文件 | 任务 |
|----|------|------|
| 后端·实体 | `src/XiaoShuTong.Domain/Entities/TaskManagement/TaskAssignments.cs` | T1（ConsumedQuestionIds 内部字段） |
| 后端·服务 | `src/XiaoShuTong.Domain/Services/Learning/SubmitAttemptService.cs` | T2（SyncTaskProgressAsync 增量改造 + ParseConsumedSet + RebuildIfDriftedAsync） |
| 业务规则 | `.TKWF/Business.md` + `.TKWF/LOG.md` | T3（BR-24 实现口径追加 + 避坑指南） |
| 后端·测试 | `tests/XiaoShuTong.Tests/Learning/SubmitAttemptServiceTests.cs` | T4（+6 用例：跨会话累积/同题幂等/达上限不重复/幽灵重建/损坏串降级/空集合升级收敛） |
| 文档 | `docs/adr/ADR-010-任务进度联动契约.md` + `docs/变更记录.md` | T5（ADR 登记 + 变更记录） |

## 二、审核结论

**总评：通过**

V0.6.1 无遗留阻断项。后端证据完整：全套件 **389 通过 / 0 失败 / 2 跳过**（基线 383 净增 +6，T4 补充用例全绿）、`dotnet build` 0 错误。**契约零变化实测通过**：`ConsumedQuestionIds` 全仓 grep 仅命中 2 个手写源文件（Entity + Service），`schema.graphql` 的 `TaskAssignmentsDto`（L1569-1595）字段集与 V0.6.0 完全一致（无新字段泄漏），ts-client/AdminWasm/WebH5 消费端不受影响。BR-24 判定口径、状态迁移、Completed 幂等、个人会话豁免**全部不变**，仅实现路径优化。

**Oracle 评审闭环达标**：方案附条件通过 7 条意见全部闭环——#1（阻断·QuestionId 全域 string，`HashSet<string>(StringComparer.Ordinal)` 修正）、#2（ADR-010 实现路径增补登记）、#3（并发论证修正：不宣称与全量重算等价，改为单活动会话 UX 假设 + 校验兜底）、#4（校验触发放宽 `set.Count >= totalCount-1` 防漂移漏触发）、#5（Position 连续核实 1-12→13 + StringLength 1000→2000）、#6（补损坏串降级 + 升级兼容两用例）、#7（schema.graphql diff 为空实测确认）。

## 三、需求符合度

### 方案 §一 目标 1 项 → 实施

| 目标 | 实现状态 | 依据 |
|------|---------|------|
| 1. 重算热路径优化：O(N²)→增量集合 + 完成时校验 | ✅ 完成 | SyncTaskProgressAsync 重构（只查本题 + ParseConsumedSet 集合读写 + RebuildIfDriftedAsync 近完成校验）；每次作答查询量从"全任务 attempts 全量扫描"降为"taskSessions(O(1-3)) + 本题 attempts(O(1-2))" |

### T1-T5 逐任务对照表

| 任务 | 验收条件（方案 §六） | 实现状态 | 依据 |
|------|--------------------|---------|------|
| T1 实体字段 | Position=13 连续 + [DtoFieldIgnore][JsonIgnore] 不进 Dto + build 0 错误 | ✅ | TaskAssignments.cs 新增 ConsumedQuestionIds（Position 13，StringLength 2000）；域 grep 证实零生成物泄漏 |
| T2 服务改造 | 只查本题 + 集合读写 + 近完成校验 + build 0 错误 | ✅ | SyncTaskProgressAsync（string questionId 参数 + 单题过滤 + OrderBy 序列化）+ ParseConsumedSet（损坏串安全降级）+ RebuildIfDriftedAsync（全量校验重建） |
| T3 Business.md | BR-24 口径不变 + 实现路径追加 + LOG.md | ✅ | BR-24 行追加"（V0.6.1 实现路径：增量集合……）"；LOG.md 2026-09-25 条目（3 条避坑指南） |
| T4 测试 | 7 用例全绿 + 基线不降 | ✅ | ExecuteAsync_IncrementalAcrossSessions / SameQuestionRepeated / WrongAtLimitThenCorrect / GhostConsumed / CorruptedConsumedSet / UpgradeFromEmptySet——39/39 全绿，全套件 389/0/2 |
| T5 文档 | ADR 登记 + 变更记录 + 审核报告 | ✅ | ADR-010"实现路径增补 + V0.6.1 完成"两节；变更记录 V0.6.1 条目；本报告 |

## 四、架构符合度

| 架构约束（ADR-010 / 方案 §五） | 遵守情况 | 说明 |
|-------------------------------|---------|------|
| ADR-010 决策一：重算式计数（非 Progress+=步进） | ✅ | Progress 仍 = `consumedSet.Count / Tasks.QuestionCount × 100`（重算式），非步进累加；持久化集合幂等 Contains + 近完成全量校验保留"抗漂移"优势 |
| ADR-010 决策二：完成判定 Correct\|count≥MaxAttempts | ✅ | 只查本题后同口径判定（questionAttempts.Any(Correct) \|\| Count >= MaxAttempts） |
| ADR-010 决策三：跨会话聚合 | ✅ | sessionIds 桥接保留，单题过滤仍跨会话（支持 BR-05 续做/BR-23 逾期复习） |
| ADR-010 决策四：状态迁移 | ✅ | Pending→InProgress / ≥100→Completed / Completed 幂等——不变 |
| ADR-010 决策五：三预留字段 | ✅ | SessionId/StartedAt/CompletedAt 接线不变 |
| 域边界 | ✅ | 增量派生仍归 SubmitAttempt（ADR-009/ADR-010 既定）；无新增跨域写 |
| 契约零变化 | ✅ | `[DtoFieldIgnore][JsonIgnore]` 双保险；schema.graphql TaskAssignmentsDto 字段集实测与 V0.6.0 一致 |
| POCO 管控 | ✅ | 无新建 POCO；仅实体内部字段 + 两个私有方法 |

## 五、代码质量

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 类型安全 | ✅ | QuestionId 全域 string：`HashSet<string>(StringComparer.Ordinal)`、`string.Join(",", OrderBy(StringComparer.Ordinal))`；无 as/long 强转 |
| 异常处理 | ✅ | ParseConsumedSet `Split(RemoveEmptyEntries\|TrimEntries)` 对损坏串（`,,Q,,,`）安全降级不抛；集合圆整 + Clamp(0,100) |
| 业务规则可溯源 | ✅ | BR-24 判定口径逐字保留；消费判定/状态迁移/豁免路径对照 Business.md 学习-BR-24 行 |
| 生成物纪律 | ✅ | 实体经 xCodeGen AfterBuild 自动重生成（7/0 骨架）；未手写 .g.cs/Dto/DataService/Controller |
| 并发/幂等 | ✅ | 集合 Contains 幂等（同题重复不 +1）；近完成全量校验重建防并发/幽灵/升级漂移；Completed 幂等早退不变 |
| 查询复杂度 | ✅ | 每次作答 O(会话数+本题尝试数) ≈ O(1-2)，去除全任务 attempts 全量扫描（ADR-010 决策一"查询成本可控"优化落地） |

## 六、测试情况

### 自动化测试（2026-09-25 复跑）

- 命令：`dotnet test tests/XiaoShuTong.Tests/`（Tier 1.5 SQLite 内存库）
- 结果：**389 通过 / 0 失败 / 2 跳过**（基线 383 +6；2 跳过为 XiaoShuTongServiceTests 模板示例）
- SubmitAttemptServiceTests 分类：39/39 全绿（原 V0.6.0 的 7 个 BR-24 用例保持，+6 新增）

### T4 新增用例覆盖矩阵

| 用例 | 场景 | 断言核心 |
|------|------|---------|
| IncrementalAcrossSessions | 3 题跨 2 会话续做 | Progress 33→67(Round)→100；集合排序序列化；Completed+CompletedAt |
| SameQuestionRepeated | 同题 Correct 再答（不同答案防幂等） | 集合 Contains 不重复 +1；Progress 不变 |
| WrongAtLimitThenCorrect | 达上限消费后再 Correct | 不重复 +1（集合幂等） |
| GhostConsumed | seed 集合含幽灵题 | 近完成校验重建为真实消费集（幽灵清除） |
| CorruptedConsumedSet | 损坏集合串（空元素） | ParseConsumedSet 降级不抛；合法题保留；继续累积 |
| UpgradeFromEmptySet | 空集合 + 预存旧 Attempts | 首次作答触发校验收敛为全量一致（含旧题） |

## 七、遗留项（不阻断）

| # | 遗留项 | 状态 |
|:-:|--------|------|
| 1 | **并发增量丢失终极消除**（TaskAssignments 行版本号乐观并发 + 丢失检测重算） | 已登记 ADR-010 实现增补"已登记限制"；当前由单活动会话 UX 假设 + 近完成校验兜底；成本/概率不匹配，后续项 |
| 2 | **漂移周期巡检 Job**（完全杜绝"卡 <100 永不触发"极端） | 近完成触发放宽已缓解；周期 Job 成本不匹配，登记后续项 |
| 3 | 真实后端端到端联调 | 沿用历迭代优先建议，另立 |
| 4 | 任务题集变更后的进度重算基准 | V0.6.0 已排除，仍有效 |

## 八、结论

V0.6.1 达成 DoD 全部验收项：

- [x] `dotnet build` 0 错误（Domain + Tests）
- [x] 全套件 **389/0/2**（基线 383 +6，不降反增）
- [x] T4 新增 6 用例全绿
- [x] TaskAssignmentsDto 无 ConsumedQuestionIds（域 grep 仅 2 手写源文件命中）
- [x] **schema.graphql diff 为空**（TaskAssignmentsDto L1569-1595 字段集与 V0.6.0 一致）
- [x] Business.md BR-24 实现口径已追加；LOG.md 已记录
- [x] ADR-010 实现路径增补已登记（Oracle 闭环 #2）
- [x] 变更记录 V0.6.1 条目已追加

**总评：通过**。热路径从 O(N²) 全量重算收敛为 O(1) 增量维护 + 近完成一次全量校验，且对外契约（Dto/schema/ts-client）零变化、BR-24 判定语义零漂移。可进入 V0.6.1 提交推送流程。