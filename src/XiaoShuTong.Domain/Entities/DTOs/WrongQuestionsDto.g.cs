// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: cdaecd67127209b0ec5e8eaac8800e4b36d1f9b7ceeb452dc91f8d5da0f48141]
// 模型: WrongQuestions (XiaoShuTong.Entities.Learning.WrongQuestions)
// 生成时间: 2026-09-08 05:19:32
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using XiaoShuTong.Entities.Learning;

namespace XiaoShuTong.Entities.Learning.DTOs;
/// <summary>条件工厂（V4.9）。支持两种使用方式：     <list type="bullet">     <item><c>WrongQuestions.Conditions.ByXxx(value)</c> — 直接返回表达式（已有代码兼容）</item>     <item><c>WrongQuestions.Where.ByXxx(value).ByYyy(value)</c> — 链式组合，隐式转换到 Expression</item>     </list></summary>
public partial record WrongQuestionsDto : IDomainDto<WrongQuestions>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 自增主键 </summary>
    public long Id { get; init; }
    /// <summary> 外部业务键 </summary>
    public required string UId { get; init; }
    /// <summary> 学生用户 </summary>
    public long UserId { get; init; }
    /// <summary> 题目业务键 </summary>
    public required string QuestionId { get; init; }
    /// <summary> 题库业务键 </summary>
    public required string BankId { get; init; }
    /// <summary> 冗余学科字段（跨学科查询免 join） </summary>
    public required string Subject { get; init; }
    /// <summary> 错误次数 </summary>
    public int WrongCount { get; init; }
    /// <summary> 最近错误时间 </summary>
    public DateTime LastWrongAt { get; init; }
    /// <summary> 连续 2 次答对 → true </summary>
    public bool Mastered { get; init; }
    /// <summary> 创建时间（框架审计字段） </summary>
    public DateTime CreateTime { get; init; }
    /// <summary> 更新时间（框架审计字段） </summary>
    public DateTime UpdateTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static WrongQuestionsDto FromEntity(WrongQuestions entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new WrongQuestionsDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            UId = entity.UId,
            UserId = entity.UserId,
            QuestionId = entity.QuestionId,
            BankId = entity.BankId,
            Subject = entity.Subject,
            WrongCount = entity.WrongCount,
            LastWrongAt = entity.LastWrongAt,
            Mastered = entity.Mastered,
            CreateTime = entity.CreateTime,
            UpdateTime = entity.UpdateTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<WrongQuestions, WrongQuestionsDto>> SelectExpression => e => new WrongQuestionsDto
    {
        Id = e.Id,
        UId = e.UId,
        UserId = e.UserId,
        QuestionId = e.QuestionId,
        BankId = e.BankId,
        Subject = e.Subject,
        WrongCount = e.WrongCount,
        LastWrongAt = e.LastWrongAt,
        Mastered = e.Mastered,
        CreateTime = e.CreateTime,
        UpdateTime = e.UpdateTime,
    };
    #endregion

    WrongQuestions IDomainDto<WrongQuestions>.ApplyToEntity(WrongQuestions entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(UId), d => d.UId, (e, v) => e.UId = v )
            .Map(nameof(UserId), d => d.UserId, (e, v) => e.UserId = v )
            .Map(nameof(QuestionId), d => d.QuestionId, (e, v) => e.QuestionId = v )
            .Map(nameof(BankId), d => d.BankId, (e, v) => e.BankId = v )
            .Map(nameof(Subject), d => d.Subject, (e, v) => e.Subject = v )
            .Map(nameof(WrongCount), d => d.WrongCount, (e, v) => e.WrongCount = v )
            .Map(nameof(LastWrongAt), d => d.LastWrongAt, (e, v) => e.LastWrongAt = v )
            .Map(nameof(Mastered), d => d.Mastered, (e, v) => e.Mastered = v )
            .Map(nameof(UpdateTime), d => d.UpdateTime, (e, v) => e.UpdateTime = v )
        ;
        return entity;
    }

    WrongQuestions IDomainDto<WrongQuestions>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(WrongQuestionsDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<WrongQuestions>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<WrongQuestions, WrongQuestionsDto>(scene, results)
                .MaxLength(nameof(UId), 32, d => d.UId)
                .MaxLength(nameof(QuestionId), 64, d => d.QuestionId)
                .MaxLength(nameof(BankId), 64, d => d.BankId)
                .MaxLength(nameof(Subject), 32, d => d.Subject)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class WrongQuestionsDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static WrongQuestionsDto ToDto(this WrongQuestions entity) => WrongQuestionsDto.FromEntity(entity);
}

